# Coder Notes — #303 Configurer tsup bundler (ESM+CJS+types)

## Fichiers modifiés

| Fichier | Description |
|---|---|
| `packages/react/tsup.config.ts` | Config tsup : entry src/index.ts, format ESM+CJS, dts, sourcemap, clean, external react/react-dom, treeshake, target es2020 |
| `packages/react/tsconfig.json` | tsconfig minimal compatible tsup : ES2020, moduleResolution bundler, jsx react-jsx, declaration true |
| `packages/react/package.json` | Ajout scripts (build/dev/clean/prepublishOnly) + devDeps tsup@8.5.1 + rimraf@6.1.3 |
| `.gitignore` | Ajout packages/react/dist/ + packages/react/node_modules/ |

## Divergences avec la spec

Aucune. Tous les acceptance criteria de l'issue #303 sont couverts :
- tsup.config.ts présent avec ESM + CJS + types + treeshake + sourcemap + external
- src/index.ts barrel export stub en place (livré par #302)
- npm run build produit dist/index.js, dist/index.cjs, dist/index.d.ts (+ .d.cts + sourcemaps)
- React + react-dom marqués comme externals
- Smoke test ESM + CJS OK

## Points d'attention pour le tester

- Le build produit aussi `index.d.cts` (types CJS) et sourcemaps — c'est normal avec tsup
- Le stub `export {}` génère des "Generated an empty chunk" warnings dans tsup — attendu, pas une erreur
- `packages/react/node_modules/` ajouté au .gitignore racine pour éviter de commiter les deps locales du workspace

## Tests adaptés par le coder

Aucun test préexistant sur packages/react (package créé en #302). Build validé + smoke test ESM/CJS (import ESM OK, require CJS OK, exports: []).
