# Coder Notes — #278 + #282 — Registry auto-généré + utility .icon-svg

## Fichiers modifiés

| Fichier | Action |
|---|---|
| `bin/generate-registry.js` | Créé — script Node.js ZÉRO HARDCODE, scanne shared/css/**/*.css |
| `shared/components-registry.json` | Régénéré — v2.58.0 → v2.59.0, +23 groupes, +704 classes (1115 total) |
| `shared/css/utilities.css` | Ajout `.icon-svg` + bump @ds-version 2.59.0 |
| `shared/CONSUMER_GUIDE.md` | Section `## Utilities` ajoutée avec doc `.icon-svg` + exemple |
| `package.json` | Script `generate-registry` ajouté + version 2.59.0 |
| `shared/css/tokens.css` | Bump @ds-version 2.59.0 |
| `shared/css/components.css` | Bump @ds-version 2.59.0 |
| `shared/css/layout.css` | Bump @ds-version 2.59.0 |
| `shared/nav.js` | Bump @ds-version 2.59.0 |

## Divergences avec la spec

- Spec demande bump version dans `components-registry.json` et `package.json` uniquement. Le coder a également bumpé les 5 fichiers `@ds-version` (tokens.css, utilities.css, components.css, layout.css, nav.js) conformément à la convention DS documentée dans CLAUDE.md. Nécessaire pour que `check-sync.sh` rapporte la bonne version aux consumers.
- Le script exclut `themes.css` du scan (ne contient que des variables CSS `:root`, aucune classe) et les barrels (`components.css`, `components-core.css`). Conforme à l'esprit de la spec.
- Le registry passe de 50 composants manuels à 98 entrées (50 manuels préservés + 23 groupes auto-générés avec 704 classes nouvelles). Les 50 composants existants conservent leurs metadata enrichies.

## Points d'attention pour le tester

1. **Idempotence** : `node bin/generate-registry.js` lancé 2x consécutifs doit reporter "Aucune nouvelle classe" la 2e fois. Validé.
2. **check-sync.sh** sur aksy rapporte DRIFT v2.56.0 → v2.59.0 — c'est NORMAL, le consumer n'a pas encore été synchronisé. Pas un bug.
3. **check-components.sh** sur aksy passe à 0 warning. Valider sur acssistender et aksyva.
4. **Classe `.icon-svg`** : vérifier qu'elle n'entre pas en conflit chez un consumer. L'actuelle DS-EXCEPTION aksy L628 peut désormais être retirée.
5. **pnpm generate-registry** ou `node bin/generate-registry.js` depuis la racine du projet.

## Tests adaptés par le coder

Aucun fichier `*.test.*` existant ne couvre `generate-registry.js` ou `utilities.css`. Pas de test à adapter.
