# .coder-notes.md — feat/m3-health-version-endpoints (PR #293)

## Fichiers modifiés

| Fichier | Changement |
|---|---|
| `health.txt` | Créé — contenu `ok\n` (3 octets) |
| `version.txt` | Créé — contenu `2.57.1\n` |
| `package.json` | Bump version `2.56.1` -> `2.57.1` |
| `RELEASES.md` | NON MODIFIÉ — bloqué par hook pipeline (fragment dans description PR#293) |

## Divergences avec la spec

- **Version** : la spec demandait `2.57.0` mais cette version était déjà documentée dans RELEASES.md (LoginScreen #285, commit `961f21a`). Le `package.json` etant encore a `2.56.1`, j'ai applique `2.57.1` pour respecter la sequence sans collision.
- **RELEASES.md** : hook `hook-block-coder-mutations.py` a bloque la modification directe. Le fragment est dans la description de PR#293 pour consolidation post-merge par le parent /sprint.

## Points d'attention pour le tester

1. **Caddy rewrite** : sans modification du bloc Caddy, `health.txt` et `version.txt` sont servis comme `site.html` (SPA rewrite). Les fichiers existent en repo mais les URLs `https://design-system.msyx.fr/health.txt` et `/version.txt` retournent le HTML du DS. C'est attendu en Phase 1 — la Phase 2 corrige ca.
2. **Verification post-Phase 2** : apres ajout des blocs `handle /health.txt` et `handle /version.txt` dans le Caddyfile, tester `curl https://design-system.msyx.fr/health.txt` (doit retourner `ok`) et `curl https://design-system.msyx.fr/version.txt` (doit retourner `2.57.1`).
3. **Uncommitted changes** : gh pr create a averti "2 uncommitted changes" — il s'agit des repertoires `.claude/` et `auth-static/` (untracked, non commites, hors scope).

## Tests adaptes par le coder

Aucun — fichiers statiques, pas de logique, pas de tests existants touches.
