# memory.md — design-system-project
# Niveau 3 — État session

> Format borné (skill /memory-update). Détails complets = auto-memory Claude Code (`memory/`) + RELEASES.md/CHANGELOG.md.

## Contexte courant
- **2026-07-27 (run autonome nuit) — #758 XSS LIVRÉ + Sprint 4 SOLDÉ : le DERNIER milestone du projet est fermé.**
- `main` = `63c9794`, DS **2.116.3**, **`@msyx-dev/react@3.0.0-alpha.26` publié** (workflow `publish-react.yml` success), préprod **FINISHED**, `running:healthy`.
- **Epic #396 → Done (78/78 sous-issues)**, **Milestone #41 « Parité React » fermé** → **plus aucun milestone ouvert** sur le projet.
- **#758 (audit XSS)** : la cause racine n'était PAS celle décrite au ticket. `escapeHTML()` sérialise via `innerHTML` → n'échappe que `& < >`, **jamais les guillemets** → tout appel en contexte d'ATTRIBUT est contournable. Les 3 vecteurs les plus graves (`initUserMenu`, et dans `nav.js` `updateHeaderUser` + `renderNotifications`, tous alimentés par des données consumer) étaient **absents de l'inventaire** parce qu'ils *avaient déjà* un échappement — invisibles au `grep innerHTML`. **11 vecteurs corrigés** par construction de nœuds (jamais d'`escapeHTML` ajouté), + helper **`safeUrl()`** (les schémas `javascript:` passent `setAttribute` sans encombre), + garde-fou CI **`bin/check-innerhtml.js`** (fail-closed, couvre aussi `constantMarkup()` appelé avec autre chose qu'un littéral), + règle gravée **`DS-PRINCIPLES.md` §11** + rappel condensé dans le `CLAUDE.md`. Périmètre élargi à `nav.js` en cours de route (le laisser dehors aurait fait poser des dérogations de lint sur du code vulnérable). 23 dérogations `ds-allow-innerhtml`, toutes motivées et relues une par une.
- **Sprint 4** (4 ports, tests React **1209 → 1339**) : **#596** `<JsonViewer>` · **#595** `<SplitPane>` · **#760** `<Calendar>` single/range · **#761** `<TimePicker>`. **#628 scindé en #760/#761** avant dev (il lumpait ~11 SP). Plus **#762/#763** (hygiène DS, 2.116.3).
- **Écart de parité rattrapé en review sur #760** : le coder avait fidèlement reproduit le fait que le vanilla n'émet `calendar:change` qu'en plage complète — mais le vanilla **re-render à chaque clic**, donc le 1er clic EST visible. En React contrôlé sans notification, l'écran restait vide → `onChange` émet désormais `{start, end: null}` à chaque étape.
- **2026-07-26 (fin de session) — #746 XSS CORRIGÉ (DS 2.116.1)** : les 2 `highlightMatch` (`initSearchInputs` + `initMentionInput`) factorisés en **une** fonction qui **construit les nœuds** (`createTextNode`/`createElement('mark')`/`appendChild`) — `innerHTML` supprimé, pas seulement échappé (un échappement manuel se contourne dès qu'un appel futur l'oublie). Mergé sur `main` (`e4d2282`), versions cohérentes (`package.json` = `version-notes[0]` = 2.116.1).
- **#758 ouvert (P1 Security) — l'audit du coder suggère que #746 n'était PAS isolé** : `shared/components.js` compte **46 `innerHTML`**, dont la légende pie/donut qui injecterait `chart.dataset.labels` (attribut **du consumer**). ⚠️ **Inventaire NON vérifié ligne à ligne** (numéros de ligne non reproductibles depuis `main`) — le ticket le porte explicitement comme piste d'audit, pas comme constat. À trancher aussi : `virtual-list` reçoit un callback de rendu du consumer → contrat d'API assumé (comme `bodyHTML` du modal) ou faille ? Si assumé, à DOCUMENTER.
- **2026-07-26 — #613 (convention ARIA) + Sprint 3 « React interactifs » LIVRÉS.** Préprod **FINISHED** sur `321f829`, DS **2.116.0**, **`@msyx-dev/react@3.0.0-alpha.25` publié** (workflow `publish-react.yml` success). Tests React **1071 → 1209**. Milestone Parité React : **3 issues réelles restantes** (#628 calendar, #596 json-viewer, #595 splitter) + Epic #396.
- **#613 — convention ARIA `radiogroup` tranchée (décision Mike) et GRAVÉE** dans `docs/DS-PRINCIPLES.md` **§3.2** (pattern retenu + 2 alternatives écartées avec motif, format ❌/✅). Le DS portait **4** conventions divergentes pour `.segmented` (pas 3 comme annoncé) : `role=button`+`aria-pressed` (JS), `role=group` (démos), `role=tablist` (registre), `radiogroup` (React). Alignement complet + AM/PM du time-picker (qui n'avait **aucun** ARIA : pas de `.segmented-indicator` → `initSegmentedControls` sortait au `return`). DS 2.116.0 + react alpha.24.
- **Review adversariale = rentable** : elle a trouvé que le garde-fou APG posait `tabindex="0"` sur un `<button disabled>` — or **un bouton désactivé reste hors tab-order quel que soit son tabindex** → le groupe entier devenait inatteignable (0 tabstop), pas « le mauvais item focusé ». Corrigé via `enabledItems()[0]`. Elle a aussi trouvé **F4** (divergence React, → #743, livré le jour même).
- **Sprint 3 (6 tickets)** : **#745** (CI React rendue bloquante + workflows déclenchés sur `integration/**`) · **#743** (garde-fou roving tabindex React) · **#468** `<ContextMenu>` (classe `.show`, ≠ `.open` d'ActionMenu) · **#461** `<Accordion>` (`.open` sur le PARENT `.accordion-item`) · **#600** `<SplitButton>` (primitif `.menu`, 1er composant React en classes BEM `__`) · **#594** `<MentionInput>` (découpe pur/DOM, **ne reproduit PAS la XSS** du vanilla).
- **6 tickets de dette créés** (tous issus du groom/review, aucun prévu) : **#746 XSS `highlightMatch`** (`innerHTML` sur texte consumer, **2 occurrences** `initSearchInputs`+`initMentionInput`, exploitabilité démontrée, distribué aux consumers via `sync.sh`) · #747 (`extractReactClasses` ignore les classes `__`) · #748 (champ `example` du registre non validé — 3 composants audités, 3 exemples faux/incomplets) · #749 (chevron accordéon sans classe → rotation morte dans la vitrine) · #750 (`.context-submenu` sans règle d'état → sous-menu inatteignable au clavier) · #744 (couverture de test du JS vanilla).
- **2026-07-25 — Revue de backlog + Sprint 1 (assainissement) + Sprint 2 (graph I6) LIVRÉS.** Préprod **FINISHED** sur `4f09247`, DS **2.115.0**, `@msyx-dev/react` **alpha.20→alpha.23 publiés**. **Milestone #45 « Brique graphique » SOLDÉ (23 issues) + Epic #656 FERMÉ.** Backlog **21 → 9 tickets** (8 réels + Epic #396), **1 seul milestone ouvert** : Parité React (71 fermés / 9 ouverts).
- **Revue de backlog — 4 tickets fermés** (validés Mike) : #459 (DataGrid déjà livré alpha.14), #681 (doublon de #704), #663 (conteneur redondant, scindé en #676/#677), #471 (écart obsolète — résolu par le primitif `Icon` #713). Challenge = croiser chaque ticket avec l'état RÉEL du code, pas son intitulé.
- **Parité header** : #731 (`UserFeedbackButton` émettait `.btn-icon` → 44×44 vs cloche 34×34 ; retrait, `.header-notification` seul) + #732 (sparkle absent du `.version-badge` React ; glyphe `sparkles` ajouté au primitif `Icon`) → **alpha.20**.
- **Sprint 1 « Débloquer & assainir »** : **#703** (tsup n'émettait aucun `"use client"` → barrel incassable en Server Component ; le `banner` esbuild est STRIPPÉ silencieusement → fix par post-traitement `onSuccess`, alpha.21) · **#704** (barrel `shared/styles.css` en `@import url()` droppé par Tailwind v4/Lightning CSS + tokens fantômes `--space-1`/`--space-3` → DS 2.113.3 ; débloque les band-aids de `feedbacks` et `cap-transfo`) · **#707** (compteurs, DS 2.113.4).
- **#707 — périmètre élargi volontairement** : le ticket parlait de « réconcilier des compteurs », l'audit a révélé **7 chiffres faux affichés aux visiteurs** sur `site.html` (version v2.110.1, et 6 compteurs de sections de hub-cards). Le coder avait *restreint* `check-counters.sh` (ses autres checks échouaient déjà sur main) → **refusé** : câbler un script amputé en CI aurait figé la dette pour toujours. Livré : `bin/generate-counters.js` (dérive compteur + sections + version depuis registre/`pages/*.html`/`nav.js VERSION`, même fonction pour génération et `--check`) + couverture complète restaurée + **gate CI bloquant**.
- **Sprint 2 « Graph I6 »** : **#697** (undo/redo tactiles, glyphes `undo-2`/`redo-2` au sprite 58→60, état disabled branché sur `graph:history:change` déjà émis — zéro réimplémentation, DS 2.114.0) · **#676** (`<Graph>` wrapper React, moteur **inliné** par tsup depuis `shared/graph/` — prouvé : 0 import externe résiduel, bundle 197→294 KB, alpha.22) · **#664** (export SVG tokens+`<use>` résolus / PNG hi-DPI / print-PDF `@media print` 0 KB / round-trip JSON `schemaVersion:1` / import Cytoscape+DOT, DS 2.115.0) · **#677** (presets Mindmap/OrgChart/DependencyMap + **mode édition exposé**, alpha.23).
- **Écart spec ↔ moteur (#676)** : la spec de #663 datait d'AVANT l'implémentation — elle annonçait `.graph-node.selected` et `onSelect(node)`, le moteur pose `.graph-node--selected` et émet `{id,kind}`. Un wrapper suivant la spec aurait été **invisible au CSS** (bug `<ActionMenu>` `.open` rejoué). Les tests assertent la classe réelle ET l'absence de `.selected`.
- **2026-07-24 — Chantier header standard `<SiteHeader>` + finalisation UserFeedback LIVRÉ (run autonome nuit).** main à jour, CI+visual verts, **`@msyx-dev/react@3.0.0-alpha.19` publié** (GitHub Packages, run publish-react success), DS statique **2.113.2**. **9 tickets mergés** : #713 (primitif `Icon` interne — 7 composants React self-contained, plus aucun `<use href=sprite>`, alpha.15) · #714 (feedback → `FileUpload`, suppression capture live `getDisplayMedia`, alpha.16) · #717 (`NotificationBell` cloche+panel+mark-all-read, alpha.17) · #716 (`<SiteHeader>` composition header standard, identité via props, alpha.18) · #725 (**toggle clair/sombre STANDARD** toujours présent + `paletteSwitch` opt-in, alpha.19) · #718 (doc CONSUMER_GUIDE + 2 sections vitrine, warn #511 résolu, DS 2.113.0) · #721 (header vanilla libellé feedback aligné, DS 2.112.2) · #727 (doc CONSUMER_GUIDE alignée paletteSwitch + self-contained, DS 2.113.1) · #729 (`version-notes.json` réaligné + badge header, DS 2.113.2). Chantier #712 (SiteHeader unifié) = **3/3 sous-issues DS livrées**.
- **Décisions autonomes tranchées** (retour Mike ou documentées sur les issues) : #713 primitif `Icon` centralisé (vs outillage distribution sprite) ; #714 **supprimer** la capture live → `FileUpload` + validation simple (image/* ≤5 Mo, non bloquante) ; #716 identité 3 états (`undefined`=loading/`null`=anonyme/objet=connecté) + features opt-out ; #718 mount `SiteHeader` **class-based** (`querySelector('.site-header')`, résout collision d'id — le shell garde la classe, VR masque `.site-header` donc mount hors-VR) ; #725 **toggle clair/sombre standard** (Mike : doit être présent par défaut comme le vanilla ; palette = `paletteSwitch` opt-in).
- **Préprod redéployée ×2** (2026-07-24, Mike a dit go) : `coolify-deploy.sh design-system --wait` → **FINISHED**, `design-system-preprod` running:healthy, commit servi **`99b251a`** (DS **2.113.2**). Vitrine `SiteHeader`/`NotificationBell` servie sur `design-system.miklaw.fr` (vérif visuelle finale = geste Mike, Authentik-gated). NB : le DS préprod se déploie via **Coolify** (`coolify-deploy.sh`), pas « Caddy checkout local » (mémoire obsolète corrigée).
- **#729 — version-notes.json réaligné** (retour Mike : « redéployé sans release note dans la feature prévue ? ») : le badge notes de version (header, #614/#645) affichait 2.112.1 vs DS 2.113.1 → 3 entrées curées (2.113.2/2.113.0/2.112.2) + bump 2.113.2, badge = 2.113.2. **LEÇON PROCESS** : les PR DS mettent à jour `RELEASES.md`/`CHANGELOG.md` (hook) mais **PAS `shared/version-notes.json`** (non forcé) → à chaque release DS, réaligner `version-notes.json` (+ `node bin/generate-version-notes.js`) sinon le badge header ment. Vérifier `released[0].version == @ds-version` avant de dire « déployé ». Cf. [[reference-version-notes-process]].
- **Reste (gestes Mike)** : fermer #712 (chantier DS soldé) + ouvrir sous-tickets **migration consumers** (cap-transfo/feedbacks) vers `<SiteHeader>` sur leurs repos ; polish V2 SiteHeader optionnel (findings review #716 : `DEFAULT_BRAND` hardcodé, état anonyme sans CTA, garde double-Provider).
- Détails : [[reference-vr-soft-harvest-recipe]] (cascade navigation harvestée par expect.soft) · [[project_state_2026-07-23-user-feedback-vitrine]].

## Prochaine étape
- **Plus aucun milestone ouvert.** Prochain jalon = à définir avec Mike.
- **Dette prioritaire** : **#765** (garde-fou CI classes posées par le JS mais absentes du CSS — **3 occurrences** : chevron accordéon #749, `.split-pane--dragging` #763, `.json-node--last` ; démarrer warn-only) · **#770** (registre lumpe `calendar`+`time-picker`, `initTimePicker` non répertorié ; scinder fait passer le compteur 109→110 → gate CI + dérive VR sur `site.html`, à faire à froid) · #747 · #748 · #744.
- **En attente d'arbitrage Mike** : purge des **4 branches locales obsolètes** (`chore/vr-baseline-refresh-drift-0714`, `feat/465-react-search-input`, `feat/password-toggle-435`, `review-686`) — vérifiées une par une contre l'état réel de `main`, toutes livrées, commits absents seulement à cause du squash-merge. Purge sans perte, **non exécutée** faute d'autorisation explicite (action irréversible).

## Décisions permanentes
- **Sécurité innerHTML** (2026-07-27, #758) : toute donnée consumer atterrissant dans un attribut passe par la **construction de nœuds** (`createElement`/`setAttribute`/`textContent`), jamais par `escapeHTML` — qui n'échappe pas les guillemets. Les URL consumer passent par `safeUrl()`. Gate CI `bin/check-innerhtml.js` bloquant, dérogations `// ds-allow-innerhtml: <raison>` motivées. Règle complète : `DS-PRINCIPLES.md` §11.
- **DS = préprod-only** (2026-07-23) : servi UNIQUEMENT sur `design-system.miklaw.fr` (Coolify, 100% Authentik-gated) ; `design-system.msyx.fr` N'EXISTE PAS (décommissionné, CLAUDE.md corrigé). Release note = **alimenter+dater+versionner directement** (pas de `[Unreleased]` qui traîne, pas de promotion prod). Ne jamais dire « livré » sans vérifier que l'user peut le VOIR (URL réelle + auth + commit déployé FINISHED). Cf. [[reference-ds-deploy-preprod-only]].
- **Moteur graph** (2026-07-19) : cœur maison mince + **dagre vendoré** + rendu **SVG** ; découplage strict layout≠rendu ({x,y} purs → renderer tokenisé). **V1 COMPLÈTE livrée v2.98→v2.115 + react alpha.22/.23 — Epic #656 fermé, milestone #45 soldé (23 issues)** : modèle/rendu, viewport, 6 layouts, a11y clavier+SR, édition+undo/redo, wrapper React + presets, export/import. Cf. [[project_graph_engine_architecture]].
- **Compteur de composants = `kind === "component"` du registre** (2026-07-25, #707) : **109** (les 29 autres entrées sont `module`/`pattern`/`layout`/`utility`, pas des composants UI). L'ancien `check-counters.sh` comptait `page != null` = 111. Tous les chiffres de `site.html` (compteur, sections des hub-cards, version) sont **dérivés du code** par `bin/generate-counters.js` et gardés par un **gate CI bloquant** — ne plus jamais les saisir à la main.
- **Export graph sans dépendance** (2026-07-25, #664) : print-PDF via `@media print` (0 KB) ; le **PDF programmatique (jsPDF/svg2pdf) reste HORS PÉRIMÈTRE** tant que les licences ne sont pas vérifiées. Tokens `--graph-print-*` volontairement hors theming (un thème dark imprimé = traits clairs sur papier blanc), déclarés dans `tokens.css` car `check-hardcoded-tokens.sh` refuse les hex en dur.
- **2 artefacts versionnés séparément** (#314) : DS CSS (`@ds-version`, `RELEASES.md` racine, push main) + `@msyx-dev/react` (`3.x-alpha`, tag `react-v*` → `publish-react.yml`). JAMAIS mélanger les entrées RELEASES.
- **User Feedback = standard** (2026-07-23) : le bouton feedback est présent PAR DÉFAUT dans le header DS (`nav.js buildHeader`) — le header DS = image du header par défaut d'un nouveau projet. Connecté → email pré-rempli visible (via `MSYX_HEADER.user` ou `__updateFeedbackAuthState` en M3 async).
- **VR (visual.yml)** : ne capture que `.main > section[id]` (header hors-VR). Nouvelle page/section → **soft-harvest** des actuals CI (`local≠CI`). Cf. [[reference-vr-soft-harvest-recipe]].
- **Versioning DS** : bump synchrone 8 sources (`@ds-version` ×5 + package.json + nav.js VERSION + components-registry.json), validé `check-versions.sh`. Feature=minor, fix=patch.
- **Pipeline** : hotfix prod interdit → worktree ; parent bloqué en Edit/Write direct sur repo prod (hook N1) ; hook `hook-changelog-on-merge` exige `CHANGELOG.md` (pas seulement RELEASES) ; hook anti-destructif bloque tout Bash contenant `msyx.fr` (apex OVH) → passer les valeurs par fichier ou utiliser Edit.

## Queue fail-soft 2026-07-25 (4 entrées — à arbitrer Mike)
Détection automatique de dérive des diagrammes (session `manual`, 04:15-04:21). Aucune n'est liée à un blocage de hook sur mon travail — ce sont des rafraîchissements de doc à valider. Toutes SAFE (génération + commit/push docs).
- `infra-model-driftcheck.sh --update` + rendus + nexus → topologie infra périmée (repo `claude-config`).
- `refresh-diagrams-after-deploy.sh "cap-transfo"` → archi cap-transfo périmée.
- `refresh-diagrams-after-deploy.sh "design-system"` → **archi DS périmée — pertinente** : 7 merges + moteur graph étendu (`shared/graph/io/`) aujourd'hui.
- `flux-model-driftcheck.sh auth --update` → flux auth périmé.

## Historique sessions (FIFO max 10)
- 2026-07-27 — **#758 XSS + Sprint 4 « Parité React »** : **dernier milestone du projet soldé** (Epic #396 78/78, M#41 fermé). DS 2.116.3, react **alpha.26**. 11 vecteurs XSS + gate CI + DS-PRINCIPLES §11 ; 4 ports React (1209→1339 tests) ; #762/#763 hygiène DS. Leçons : **auditer valeur par valeur, pas bloc par bloc** (j'avais classé la tooltip risk-matrix « sûre » en vérifiant `escapeAttr` sur 2 valeurs, une 3e — `lvl` en `class=` — était brute) · **`pnpm test`/`build` ne type-checkent pas les `*.test.tsx`** → `npx tsc --noEmit` · le dernier port lancé en parallèle **rebasera** (garder les 2 côtés, vérifier que `version` du registre ne régresse pas) · **3e occurrence de classe posée par le JS sans CSS** → #765.
- 2026-07-26 — **#613 convention ARIA + Sprint 3 « React interactifs »** : 7 tickets livrés (#613/#745/#743/#468/#461/#600/#594), DS **2.116.0**, react **alpha.25 publié**, préprod FINISHED `321f829`, tests 1071→1209. **6 tickets de dette créés** dont **#746 XSS** (sévère). Leçons : **review adversariale rentable** (F3 sous-estimé par le parent + F4 non vu) · **la CI ne testait pas React** (`continue-on-error`, découvert au groom #600) et **aucune CI ne tournait sur les branches d'intégration** (workflows câblés `main` seul) → corrigés par #745 · **`RELEASES.md` est un fichier partagé en append** au même titre que `index.ts`/registre (conflit #753 ↔ #752) · **vérifier le merge AVANT `post-merge.sh`** (a fermé #468 à tort sur un merge en conflit) · **`git worktree add <branche-locale>` part d'une branche périmée** — les squash-merges atterrissent côté GitHub, toujours `reset --hard origin/...` · **le comptage brut de grep ment** (2 fausses alertes : `action-menu` = assertions négatives, `document.` = commentaire JSDoc) — lire le contexte.
- 2026-07-25 — **Revue de backlog + Sprint 1 + Sprint 2** : 4 tickets obsolètes fermés (#459/#681/#663/#471) ; #731/#732 (alpha.20) ; Sprint 1 #703 (alpha.21) / #704 (2.113.3) / #707 (2.113.4, périmètre élargi : 7 chiffres faux d'accueil + gate CI anti-récidive) ; Sprint 2 #697 (2.114.0) / #676 (alpha.22) / #664 (2.115.0) / #677 (alpha.23) → **milestone #45 SOLDÉ + Epic #656 fermé**. Backlog 21→9. Préprod FINISHED `4f09247`. Leçons : [[feedback-deploy-gc-kills-fresh-worktrees]], coder segmente systématiquement (~60-115 tool_uses) → parent finalise, challenger tout « périmètre restreint » d'un coder.
- 2026-07-24 — Chantier header standard `<SiteHeader>` + finalisation UserFeedback (run autonome nuit) : 9 tickets #713/#714/#716/#717/#718/#721/#725/#727/#729, react alpha.15→**alpha.19 publié**, DS 2.112.2→**2.113.2** ; toggle clair/sombre standard (#725, retour Mike) ; soft-harvest cascade VR navigation via expect.soft (#718) ; **version-notes.json réaligné** (#729, badge=2.113.2, leçon process) ; **préprod redéployée ×2** (coolify-deploy.sh FINISHED). Reste = vérif visuelle Mike + fermer #712.
- 2026-07-23 — User Feedback vitrine + header standard + email pré-rempli (v2.110→2.112.1) ; déphasage versions cutté ; DS préprod-only figé ; #707/#710/#711.
- 2026-07-21 — Feedback Core ① : composants React UserFeedback*+DataGrid (`@msyx-dev/react` alpha.14, milestone #46 soldé, Epic #691 Done).
- 2026-07-21 — Graph I5-3 undo/redo (v2.109.0) → lot I5 (édition) soldé, conteneur #662 clos.
- 2026-07-20 — Graph I5-1/I5-2 (édition create/delete/inline/ports) v2.107-2.108 ; I4 (a11y clavier + SR) v2.105-2.106, #661 clos.
- 2026-07-19 — Nouvelle brique graph : archi validée + backlog #656/#657-677 + moteur read-only complet v2.98→2.104.
- 2026-07-18 — version-notes montée de niveau v2.97.0→2.97.2 + `@msyx-dev/react` alpha.13 (#649/#650) ; DoD #355 posée.
