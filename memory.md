# memory.md — design-system-project
# Niveau 3 — État session

## Contexte courant
- **2026-07-05 (soir) — ROADMAP PARITÉ REACT PRÉPARÉE (demande Mike « prépare tous les sprints par logique, on passera en nouvelle session »).** Les 91 `pending` du registre cartographiés par famille (page DS) → **~11 sprints groupés par module** (classes/patterns cohérents, faible churn shared-files), ordonnés par valeur, absorbant les 23 issues déjà ticketées. Roadmap durable complète = **[[project-react-parity-roadmap]]** (auto-memory). **Sprint 0 = cleanup registre** : reclasser 7 `n-a` (reset-natif/texture-grain/brand-acssi/code-inline/avatar-img/sidebar-link-disabled/sidebar-sublinks). **Sprint 2 = runnable immédiatement** (7 issues déjà ticketées : #452 theme-switcher + #457/463/464/465/466/469). Sprints 3→12 = mix ticketé/non-ticketé (le groom crée les manquants + triage proactif-catalogue). **AUCUNE issue créée en masse** (roadmap = la logique ; ticketing par-vague au groom, certains dropperont). Décisions Mike en attente (non bloquantes) : #613 ARIA SegmentedControl + déploiement préprod DS miklaw.fr 2.94.0. **PROCHAINE SESSION = /sprint sur Sprint 2** (ou traiter #613/cleanup Sprint 0 d'abord). main=1f86434, react=3.0.0-alpha.8, ds=2.94.4.
- **2026-07-05 — 🎉 MILESTONE #41 « Parité React » SPRINT 1 LIVRÉ + PUBLIÉ npm. Dernier milestone DS soldé pour ce lot.** Autonomie complète autorisée par Mike (AskUserQuestion : « complet, npm publié » + blanket merge des PR self-authored dès CI verte). **Setup** : 9 issues React M#40 créées (#592–600, type **Feature** — aligné sur les 51 siblings de #396, PAS Task comme disait le handoff) ; **2 PR registre DS-CSS** : #602 (v2.94.3, `react:"n-a"` sur les 4 CSS-only btn-group/prose/orb/diff-viewer) + #604 (v2.94.4, **dédup color-input/color-picker** — 2 entrées identiques, gardé `color-picker` car matche `<section id=color-picker>`+nav+`example` ; kind:component 105→104, compteurs site.html 105→104). **6 ports React** mergés SÉQUENTIELLEMENT (chacun branché sur main fraîche après merge du précédent = **zéro conflit** sur fichiers partagés `src/index.ts`/`REACT_TO_REGISTRY`/`registry react:ported`) : **#453 toast** (ToastProvider+useToast, PR #605, CALIBRAGE=45tu/5min/3SP), **#454 modal** (focus restore WAI-APG, #606), **#455 tabs** (#607), **#456 action-menu** (#608), **#458 inputs** (Input/Select/Checkbox/Radio/Toggle, #609), **#467 segmented-control** (#610). **Release consolidée** : `@msyx-dev/react` **v3.0.0-alpha.7** (PR #611, tag `react-v3.0.0-alpha.7`→`publish-react.yml`→GitHub Packages ✅) bundle les 6 ports + **dette #518** (ThemeToggle réécrit `.theme-toggle`→`.mode-switch` #382, jamais publié depuis alpha.6) + Unreleased #374/375/376. **Parité 5→11 ported** (91 pending, 31 n-a). **Passe de vérification adversariale croisée** (workflow 6 vérificateurs, indépendants des tests du coder) a trouvé **1 bug BLOQUANT** : `<ActionMenu>` montait `.action-menu` **sans `.open`** → CSS DS (overlays.css `opacity:0;visibility:hidden`) laissait le menu **invisible** malgré `aria-expanded=true` ; masqué par un test qui ne vérifiait que la présence du nœud → **fix v3.0.0-alpha.8** (PR #612, `.action-menu.open` + garde-test, tag `react-v3.0.0-alpha.8` publié ✅). Verdicts : toast/tabs clean, modal/inputs/segmented mineurs → **suivi #613** (Modal aria-labelledby, Input .input-disabled, **SegmentedControl convention ARIA `radiogroup` vs `group` — DÉCISION MIKE/DS-PRINCIPLES requise** : 3 conventions coexistent déjà dans le DS vanilla). **Reste M#41** : ~91 ports pending (les 15 P1 + P2/P3 de #396 + les 9 M#40 #592–600), sprints futurs. **Mécanique React capit** : versioning React **consolidé** (1 bump alpha.N en fin de sprint, PAS par-PR → 0 conflit) ; RELEASES **séparés** (packages/react/RELEASES uniquement, `[skip-changelog]` au merge car pas d'entrée React dans CHANGELOG racine #314) ; flip `react:ported`+`REACT_TO_REGISTRY` dans la MÊME PR que le wrapper (DS-PRINCIPLES §8.1) ; port ≈ 45-55tu/5-7min/3SP via coder sonnet. **1 récidive coder** : #458 inputs a timeout à 61tu SANS commit → reprise idempotente parent (travail sauvé, push+PR). **⚠️ Préprod DS `design-system.miklaw.fr` = 2.94.0** (stale AVANT ce lot ; main servi via `serve_mode:static` git-pull = 2.94.4, config Caddy live SANS bloc design-system depuis décommission msyx.fr 2026-05-22) — le livrable sprint = npm (indépendant du serve Caddy/Coolify), **staleness préprod à clarifier avec Mike hors sprint**.
- **2026-07-04 — Follow-ups M#40 SOLDÉS ✅ (2 PR, v2.94.1 + v2.94.2) → Epic #397 + Milestone #42 CLÔTURÉS (auto post-merge).** Les 3 chips M#40 traités en `/dev` standalone (Quick Flow), fusionnés en 2 issues Task sous #397. **#588** (v2.94.1, PR #590, squash c1de8a8) = retrofit `kind:component` sur les 7 entrées registry manquantes (form-validation/btn-group/split-button/prose/orb/color-input/json-viewer) + `generate-registry` (kindComponentTotal **98→105**, 0 phantom, `module[]` auto) **+ recâblage des 3 compteurs `site.html` sur 105** (hero/meta/footer ; meta figée v2.79.0 rafraîchie). Fusion des follow-ups (a)+(c) : sans phantom, l'isolation registry-only n'apportait rien, et le compteur consomme le nouveau total. **#589** (v2.94.2, PR #591, squash 9fe2d4b) = neutralisation du lift hover `.btn-group` (`.btn-group > :hover{transform:none}`, miroir `.split-button`). Séquentiels (bump main+1 chacun, versions pré-allouées). CI **6/6 vert** les deux (job `visual` ~17-18min, **0 impact VR** : `site.html` hors matrice VR + fix hover-only). main→**9fe2d4b v2.94.2**, servi live (`serve_mode:static`, path=ce repo → `git pull` = déploy, pas de reload Caddy). Board #588/#589 Done (WARN board transitoire récurrent → relance manuelle OK). **2 leçons capit** : (1) [[feedback-self-merge-classifier-gate]] merge d'une PR self-authored bloqué par le classifier auto-mode `[Self-Approval]` — règle `permissions.allow` INUTILE (`Bash(*)` déjà présent), levé par autorisation explicite Mike **puis retry** du `gh pr merge` ; (2) [[feedback-standalone-dev-changelog]] `/dev` standalone = le coder DOIT inclure CHANGELOG+RELEASES dans son commit (hook `hook-changelog-on-merge.py` bloque le merge sinon → 2e cycle CI). `/retro #397` suggéré par post-merge (skippé : micro-epic 2 tickets, learnings en auto-memory). **Puis M#41 GROOMÉ (même session, workflow 3 recons + synthèse auto-corrigée)** : parité réelle **5/107 ported** (102 pending, 27 n-a) ; les 20 sous-issues de l'epic #396 (5 P0 + 15 P1) TOUTES valides ; **dette #518 non publiée** (npm alpha.6 sert encore l'ancienne API `.theme-toggle` alors que HEAD émet `.mode-switch`). **3 décisions Mike** prises (cf. § PROCHAINE SESSION). **Aucun dev M#41 lancé** (gate groom respectée, sprint = prochaine session).
- **2026-07-02 — 🎉 MILESTONE #40 « Composants manquants » CLÔTURÉ (Epic #395 Done 21/21, milestone fermé).** Tous les Gaps livrés en autonomie multi-vagues (demande Mike « vasy enchaine »), modèle par-composant rodé, versioning bump-par-PR. **Bilan M#40 = 15 composants**, main v2.82.0→**v2.94.0**, tous mergés + CI verte + déployés préprod. **Sprint 1** (3) : #434 table-server / #432 date-picker+time-picker / #433 form-validation. **Vague 2** (4) : #451 btn-group v2.83 / #438 split-button v2.84 / #439 .prose v2.85 / #357 .orb v2.86. **Gaps** (8) — Lot A : #448 color-picker v2.87 / #444 transfer-list v2.88 / #441 mention @ v2.89 ; Lot B : #443 splitter v2.90 / #446 json-viewer v2.91 ; Lot C : #442 heatmap-calendar v2.92 / #440 virtual-list v2.93 / #447 diff-viewer v2.94 (dernier, ce12377). **Tous zéro-dépendance** (présentation ; le consumer fournit données/calcul — analogie .prose/.code-block). **Cadence rodée par composant** : coder Sonnet (worktree) → parent review+complète (les coders SAUTENT souvent registry `kind:component`/EXPECTED_COUNTS/generate-nav-sections/CHANGELOG/ARCHITECTURE/CLAUDE + NE COMMITENT PAS → reprise parent systématique) → soft-harvest VR (patch expect.soft **via sed**, placement **fin-de-page** = churn minimal ~24 baselines : section neuve + précédente `:last-of-type`) → revert `git checkout origin/main` → bump 8 sources + entrypoint + footer → RELEASES → push → valider conclusions réelles → merge squash → post-merge + `board-update.sh … Done end` (WARN transitoire récurrent) → déploy groupé fin de lot. **Follow-ups flaggés (chips)** : (1) hover translateY `.btn-group` #451 ; (2) **retrofit `kind:component` sur 7 entrées registry M#40** (form-validation/btn-group/split-button/prose/orb/color-input/json-viewer ajoutées sans `kind` → pas validées phantom ni `module[]` dérivé ; warn-only) ; (3) drift compteurs `site.html` (mikpulse). **Compteurs site.html laissés intacts** durant M#40 (déférés au drift-reconcile — le registre porte le vrai décompte). **Reste backlog** : M#42 polish, **M#41 Parité React EN DERNIER** (surface DS désormais gelée après M#40 — déclencheur du portage). Worktrees mikpulse `cardmedia`/`wt-567` toujours présents (hors mon scope).
- **2026-06-30 — M#40 Vague 2 SOLDÉE ✅ (4/4 mergés + déployé préprod v2.86.0).** Modèle par-composant rodé appliqué 4×, séquentiel (chaque merge bumpe main+1). **#451** btn-group (`.btn-group` CSS-only, buttons.css, remplace hack inline) → **v2.83.0** (deedb29, PR #576). **#438** split-button (`.split-button` indépendant + `initSplitButton` dédié sur `.menu` #520, section neuve fin composants.html) → **v2.84.0** (b581d6a, PR #577). **#439** `.prose` (module orb… non : prose.css, rendu markdown, `:where()` scope, tables/liens hérités, section fin divers.html) → **v2.85.0** (693046a, PR #578). **#357** primitif `.orb` (module orb.css + modifs couleur/taille + `.orb--float` opt-in) + **refactor 4 usages ISO-VISUEL** (hero/login/access-denied/index.html ; hex index.html tokenisés) → **v2.86.0** (fb1f136, PR #579). **Préprod déployée** : `/version.json`=**2.86.0 sha fb1f136** (Coolify auto-trigger sur push main, deployment k9cnk380xx5u34v5vo8v1b2p FINISHED). Tous worktrees miens purgés (mikpulse cardmedia/wt-567 laissés). **Leçons VR capit (4 cycles)** : (a) **placement fin-de-page** d'une nouvelle section = churn minimal (#split-button 24, #prose 24 = section neuve + section précédemment-dernière qui gagne bordure `:last-of-type`) vs **insertion mid-page** = cascade large (#btn-group 71, #orb-démo 93 — sub-pixel sur toutes les sections suivantes) ; (b) **soft-harvest via sed** (pas Edit) évite le reformatage Prettier → revert propre via `git checkout origin/main -- visual-tests/ playwright.config.ts` ; (c) **refactor iso-visuel vérifiable** : #357 login/access-denied N'ONT PAS divergé en VR → preuve pixel-identique sur surfaces releasées (préserver tailles/opacités/blur exacts via overrides scopés + `--orb-opacity:1` sur hero pour ne pas doubler l'alpha color-mix). **2 récidives coder** : subagents créent parfois un worktree auto + NE COMMITENT PAS (#438 : 2 worktrees `wt-438` vide + `agent-…` ; #357 : pas commité) → reprise parent (rename branche, commit, compléter registry/CHANGELOG/ARCHITECTURE souvent omis). **Follow-up flaggé (chip)** : neutraliser le hover `translateY` du `.btn-group` #451 (défaut latent, non capté VR car statique). **Reste M#40** : **Gaps** A(#448/#444/#441) → B(#443/#446) → C(#442/#440-après-#434-ok/#447). Puis M#41 React EN DERNIER. Epic #395 : 8/21 sub-issues ouvertes ; milestone #40 : 9 issues ouvertes.
- **2026-06-30 — M#40 Sprint 1 SOLDÉ ✅ (3/3 mergés + déployé préprod).** #433 form-validation finalisé : bump **v2.82.0** (9 fichiers) + entrée RELEASES.md + fix réf version CLAUDE.md (`v2.81.0`→`v2.82.0`, le dev avait pré-alloué 2.81 avant que #432 ne la prenne), poussé sur PR #575. **Pré-validation locale exhaustive avant push** : check-versions (8 sources @2.82.0), check-hardcoded-tokens/css-comments/diacritics, `generate-registry --check` + `generate-nav-sections --check` (idempotents, EXPECTED_COUNTS formulaires 16→17/total 111→112), test:syntax — tous verts. Confirmé que **le bump n'affecte PAS la VR** (snapshots scopés `.main > section[id]` → header `header-version` + footer exclus ; `site.html` hors matrice VR). Le run visual « fail » initial était sur 9db539a (expect.soft, baseline form-validation manquante + password-toggle 1px) AVANT harvest ; harvest (04fa5a1)+revert(3570bbe) étaient **non poussés** → push → run frais **4/4 vert** (visual inclus). Merge squash #575 → **main 8e330e5 v2.82.0**, **main CI 4/4 vert** confirmé. post-merge.sh : board update WARN transitoire → `board-update.sh design-system-project 433 Done end 2026-06-30` manuel OK (issue close auto). **Préprod déployée** : `coolify-deploy.sh design-system --wait` → deployment tl4ibkmbkdeev74zohrngy7b FINISHED ~80s, `/version.json`=**2.82.0 sha 8e330e5**. Worktrees `wt-433`+`wt-434` purgés (mikpulse `cardmedia`/`wt-567` laissés intacts). **Drift mikpulse pré-existant signalé en tâche séparée** (chip) : `site.html` footer 88 vs hero/meta 89 composants + meta figée v2.79.0 — hors scope #433. **Reste M#40** : **Vague 2** (#451 btn-group → #438 split-button → #439 .prose → #357 .orb) puis **Gaps** A(#448/#444/#441) B(#443/#446) C(#442/#440/#447). Modèle par composant rodé : dev subagent Sonnet (push-and-return) → soft-harvest VR `expect.soft` d'emblée → harvest actuals CI → revert → valider conclusions réelles → bump main+1 + RELEASES → merge. M#41 React EN DERNIER (surface gelée).
- **2026-06-22 — M#40 Sprint 1 (dev autonome) — PARTIEL ⏳ (2/3 mergés, #433 en finalisation non poussée).** Reprise du dev M#40 en autonomie multi-vagues (demande Mike). **VERSIONING CHANGÉ → bump PAR-PR** (abandon Option B) : un chantier DS **concurrent « mikpulse S6 »** (#571/#572/#573, hors mon scope) avait avancé main de v2.77.0→**v2.80.0** par bumps par-PR pendant l'intervalle. Mike a tranché : aligner M#40 sur le bump-par-PR (#432=v2.81.0, #433=v2.82.0, …). **Livré** : **#434** table-server mergé (9cddeec, pré-mikpulse) ; **#432** date-picker INLINE + time-picker mergé (PR #566, **v2.81.0**, main=fcb17ca) — ⚠️ bug subagent rattrapé : time-picker placé dans #login → **déplacé dans #calendar** (vu au diff VR) ; **#574 « fix(vr): répare main »** mergé (67b35d5) : **mikpulse avait mergé #571-573 sur CI ROUGE** (lint nav-check #528 + visual : CSS cards/layout/utilities changé SANS harvester → **180 baselines + EXPECTED_COUNTS cassés** sur 22 sections composants/fondation/navigation). Rendus validés conformes par Mike (cards/utilities/header) → harvest 180 + fix nav-check (composants 12→13, total→111). **#433** validation : PR #575 ouverte, soft-harvest fait (24 baselines : form-validation neuve + password-toggle), patch reverté — **RESTE en local wt-433 NON POUSSÉ** : bump v2.82.0 (9 fichiers, non commité) + entrée RELEASES.md à ajouter, puis push+valider+merge. **Reste M#40** : #433 (finir) → déploy préprod Sprint 1 → **Vague 2** (#451/#438/#439/#357) → **Gaps** A(#448/#444/#441) B(#443/#446) C(#442/#440/#447). Worktrees STALE à purger : `wt-434` (mien, #434 mergé) ; `cardmedia`+`wt-567` (mikpulse, #571-573 mergés). **Leçons capit** : `gh pr checks --watch` retourne exit 0 malgré un check rouge → TOUJOURS parser les conclusions réelles (cf. [[feedback-gh-pr-checks-verify-conclusions]]) ; track concurrent peut merger sur CI rouge → vérifier l'état de main au démarrage ; recette soft-harvest VR par-PR rodée (cf. [[reference-ds-vr-and-orphan-tooling]]).
- **2026-06-19 (suite) — Milestone #40 « Composants manquants » ENTIÈREMENT GROOMÉ (epic #395) — DEV EN PAUSE (demande Mike).** #531 (header /me.json 404) corrigé+déployé entre M#44 et le groom (me.json démo à la racine, PR #564). **Backlog M#40 nettoyé par triage croisé** (workflows analystes Opus) : **4 drops fermés** (#437 toolbar = composition .filter-bar+.divider-vertical+.menu ; #445 WYSIWYG + #450 infinite-scroll = anti-patterns structurels ; #449 menu-bar = desktop hors web msyx) ; **#436 time-picker mergé→#432**. **11 composants groomés dev-ready** (grooms détaillés postés en N4 sur chaque issue, board Ready) : **Sprint 1** #432 date-picker INLINE (+time-picker #436)/#433 validation/#434 table-server (~12 SP) ; **Vague 2** #451 btn-group/#438 split-button/#439 .prose/#357 .orb (10 SP) ; **Gaps** A #448/#444/#441, B #443/#446, C #442/#440(après #434)/#447 (23 SP). **Total actif ~45 SP, tous ZÉRO-DÉPENDANCE** (le DS stylise/structure, l'app fournit données+calcul — #447 diff & #440 virtualized = couche présentation, PAS de lib ; #440 hauteur ligne fixe assumée). **Philosophie produit gravée [[feedback-ds-proactive-catalog]]** : catalogue COMPLET proactif (construire avant le besoin), genuine→build JAMAIS defer/icebox ; seuls covered-par-composition + anti-principe = drop. **Arbitrages tranchés** : #432 inline + range 1cal/2clics ; #433 API déclarative + summary réutilise .alert (#519) ; #434 initServerDataGrid SÉPARÉE + section fin ; #451 horizontal sans toggle ; #438 initSplitButton DÉDIÉ + sur .menu(#520) ; #357 primitif + REFACTOR 3 usages + anim opt-in ; #447 BUILD (présentation pure). MVP minimal + placements fin-de-page (0 cascade VR) pour les gaps (à reconfirmer au spec). **Reprise = spec+dev Sprint 1** (#432/#433/#434) comme M#44, puis Vague 2 puis Gaps A→B→C (pré-allouer versions, soft-harvest VR d'emblée).
- **2026-06-19 — Milestone #44 « Consolidation des doublons de composants » SOLDÉ (Epic #517 Done, 5/5) — préprod v2.77.0.** Sprint complet groom→spec→dev→release orchestré par le parent (workflows pour groom+spec croisés, subagents Sonnet pour dev+review, push-and-return). **5 consolidations non-breaking** (canonique élu, variantes → alias `@deprecated`→v3, tokenisation) : **#518** PR #557 (`.mode-switch` canonique, `theme-toggle.css`→alias, React `ThemeToggle` réécrit émet `.mode-switch` + `aria-checked` true===DARK #382, `REACT_TO_REGISTRY` remappé — SEULE issue `packages/react/`, release npm React **déléguée à M#41**) ; **#522** PR #558 (`tag-input` canonique, `.chip-input-*`→alias, tokens hardcodés `.tag-input-wrap` corrigés) ; **#521** PR #559 (stepper **2→1** : `.wizard-step`→`.stepper` ; `.pt-step`/`.progress-tracker-step` EXCLUS = tracker circulaire, pas steppers — titre epic «4→1» corrigé) ; **#519** PR #561 (`.alert` canonique + `.alert--kpi`/`.alert--cta` actionnable + slots, `upgrade-banner` migré hors pricing.css, tokens `--alert-*-alpha`) ; **#520** PR #562 (primitif `.menu`/`.menu-item`/`.menu-divider` nouveau module `menu.css` + `--shadow-menu`, 6 surfaces → alias, **0 churn VR** car overlays fermés + tokenisation value-preserving). Release **v2.77.0** PR #563 (bump 8 sources, tag + GH release, déployée préprod sha b5180b6). **Ordre merge** : #518 → (#521∥#522) → #519 → #520. **Versioning** : pré-allocation 2.77–2.81 (1/issue) **collapsée en 1 minor unique 2.77.0** (Option B = release consolidée parent ; refs `@deprecated`/doc pré-allouées normalisées→2.77.0). **VR — leçon majeure** : les consolidations changent la HAUTEUR de sections → **cascade de décalage** des sections suivantes sur la même page (antialiasing sub-pixel >seuil 1% `maxDiffPixelRatio`) → soft-harvest par page (#522 composants: chips+dividers+avatars+rating+segmented+sortable ; #521 formulaires: wizard+inline-edit ; #519 feedback: zone-banner+upgrade-prompt+comments+pagination). Recette `expect.soft`+`retries:0` (TEMP) capture TOUS les actuals divergents en 1 run (l'`expect` dur s'arrête au 1er échec → découverte 1-par-1 coûteuse). Cf. [[reference-ds-vr-and-orphan-tooling]]. **Incidents capit** : (a) **`gh pr checks --watch --fail-fast` retourne exit 0 même quand un check a ÉCHOUÉ** → #522 mergé avec `visual` rouge par erreur (remédié par PR harvest #560). RÈGLE : parser les CONCLUSIONS réelles (`gh pr checks` sans --watch) avant merge, jamais l'exit code du watch ; (b) subagents /dev s'arrêtent à l'UX review / quality-gate SANS ligne RESULT (récidive #518/#521/#519, #520 OK) → reprise idempotente parent (worktree a les commits, push+PR manuels) ; (c) `board-update.sh` prend le **nom** du repo (`design-system-project`) PAS `owner/repo` ; (d) subagent #519 a oublié `npm run generate-nav-sections` après modif sections feedback.html → lint manifeste #528 rouge, régén parent. **Reste backlog** : M#40 composants (#395, P0 date-picker #432/validation #433/table server-driven #434), M#42 polish (#531), M#41 Parité React (EN DERNIER ; #518 a créé une dette React à publier au démarrage M#41).
- **2026-06-16 — Milestone #43 « Taxonomie & navigation » SOLDÉ (Epic #505 Done, 15/15) — préprod v2.76.0.** Suite du sprint registre/doc : **lot churn-VR CSS** livré (v2.75.0) — refactors purs (déplacements de classes entre modules du barrel = **0 diff VR**, validé) : **#510** PR #551 (`.login-*` pricing→forms), **#512+#513** PR #552 groupés (éclatement interactive/pricing → feedback/navigation/forms/_base ; fusion modals→overlays + stub `@import`, `@keyframes fadeIn` préservé pour quiz.css). Release #554. Puis **vitrine #514** livré (v2.76.0, PR #553+#555) : split `feedback`(19→12)→nouvelle page `overlays`(7) + `motion`→`fondation`, `motion.html` supprimée, 9 pages inchangé. **Déblocage baselines VR par recette soft-harvest** : déplacer une section change son `.overline` (vrai diff que `git mv` ne couvre pas) + régén locale instable → passer `visual.spec` en `expect.soft`+`retries:0` (TEMP), 1 run CI génère TOUS les `actual`, les copier comme baselines (sections divergentes only, reste sous seuil `maxDiffPixelRatio`), revert, valider. Cf. [[reference-ds-vr-and-orphan-tooling]]. Versioning Option B reconduit (release consolidée parent). **Reste backlog** : M#40 composants (#395, P0 date-picker #432/validation #433/table server-driven #434), M#44 doublons (#517 : mode-switch #518/messaging #519/.menu #520/stepper #521/tag-chip #522), M#42 polish (#531 bug header /me.json 404), M#41 Parité React (artefact npm distinct, EN DERNIER sur surface gelée).
- **2026-06-14 (soir) — Sprint #43 axe registre & doc livré (v2.74.0, 9/9 SP, 100%).** Milestone « Cohérence taxonomie & navigation » (Epic #505), cadrage validé Mike = P0+P1 registre/doc, lot churn-VR reporté. 2 lots : **Lot 1** #506‖#507 (parallèle), **Lot 2** #508, **Lot 3** #511 (séquentiels — couplage `generate-registry.js`/`components-registry.json`). Worktrees, workers Sonnet, push-and-return, parent watch CI+merge+post-merge. **#506** v(→2.74.0) PR #546 : champ `module[]` (`string[]`, chemins repo) **auto-dérivé** par `generate-registry.js` (map inverse classe→fichier), 76 entrées peuplées (3 exemptées reset-natif/texture-grain/brand-acssi), `code-inline` normalisé, check intégrité dans `--check`, DS-PRINCIPLES §8.2. **#507** PR #545 : doc core preset = 9 modules réels (navigation incluse depuis v2.73.0, modals exclu), gzip ~14,5 KB core/~37 KB barrel ; l'audit décrivait un état pré-v2.73.0. **#508** PR #547 : 10 entrées `kind:component` (templates+data), correctif curatif pricing (`.settings-*`/`.usage-*` retirés), compteurs 78→88 ; `pie-donut` hors scope (gap résiduel, chip de suivi). **#511** PR #548 : règle frontière page↔registre DS-PRINCIPLES §6.1 + check bidirectionnel `generate-registry.js --check`, **warn-only** (49 violations name/id résiduelles = dette, `--frontier-strict` opt-in). **Release v2.74.0** PR #549 : bump synchrone 8 sources (cf. `feedback-ds-versioning-release-pattern` mémoire CC) + CHANGELOG daté + RELEASES + footer. **Versioning Option B validé** : /dev ne bump rien (8 sources restent figées → check-versions vert trivialement, parallélisation OK), release consolidée par parent en fin de sprint. Anomalie tooling récurrente : `post-merge.sh` échoue le board update #7 à chaque merge → corrigé manuellement via `board-update.sh ... Done` (à hisser claude-config). **Reste M#43** : lot churn-VR #510/#512/#513/#514 (à faire GROUPÉ = 1 churn VR + 1 release, cf. challenge). Puis M#40 composants (#395, P0 date-picker #432/validation #433), M#44 doublons (#517), M#42 #531, M#41 React en dernier (surface gelée).
- **2026-06-14 — Passe audit nocturne (rendu réel) + remédiation.** Mike avait signalé sidebar incohérente, LoginScreen débordant et charts/data fragmenté. Audit Playwright rendu réel a révélé que #509 (sidebar runtime-fetch) cassait sur préprod auth-gated (fetch → 302 Authentik → 0 sections). 4 fixes mergés autonomes : #529/v2.69.1 overflow grille `minmax(0,1fr)` (LoginScreen + data-grid + carousel + composants), #528/v2.70.0 sidebar manifeste build `NAV_SECTIONS_MANIFEST` — ZERO fetch runtime, immunisé auth-gate, #515/v2.71.0 dataviz 5 familles + `.stat-value--sm` + notes "quand utiliser", #530/v2.71.1 `html{overflow-x:clip}` + confinements (code/segmented/stepper/tabs) — 0 overflow 11 pages × 4 viewports. Vérif cumulative finale : 94 liens/0 mort. Infra VR durcie : `freezeJsAnimations` fige compteurs animés (anti-flaky). **Suite supervisée 2026-06-14** : #538/v2.71.2 (PR #539) — cause racine "absents" = 15 sections RÉELLES hors `.main` (HTML déséquilibré : fondation 6, formulaires 6, navigation 3) réintégrées → sidebar 93→108, rendu colonne ; puis #435/v2.72.0 (PR #540, remplace #537) password-toggle (1er composant M#40 livré, sur structure #538 saine) ; entrypoint→2.72.0 (cohérence /version.json). **Politique header consumers (demande Mike) : #542/v2.73.0** (PR #543) — `buildHeader` : cloche notifications + toggle dark/light **par défaut, indépendants de l'auth** ; profil (user menu) seulement si `auth:true` ; switcher thème en opt-in `MSYX_HEADER.themeSwitcher` (défaut false, vitrine DS l'active) ; `navigation.css` ajouté au barrel core ; CONSUMER_GUIDE à jour. **+ claude-config#260** : `/kickoff` choisit le thème (un seul, via `data-theme`, pas de switcher) en Phase 1 + scaffolde le header par défaut en Phase 2 (cloche/dark-light, profil si profil-auth ≥ P1). entrypoint→2.73.0. Reste : 20 composants M#40 (#395) + #531 me.json (non-bug préprod, optionnel).
- **2026-06-13 — Sprint M#43 (taxonomie & nav) livré.** 3 lots mergés sur main (worktrees, ordre séquentiel imposé par Mike) : #516 v2.67.1 (9 classes fantômes registre + validateur CI, PR #524), #523 v2.68.0 (champ react 101 entrées + CI parité React + DS-PRINCIPLES §8.1, PR #525), #509 v2.69.0 (sidebar nav.js dynamique NAV_PAGES + scan DOM, 6 liens morts + 38 orphelines, PR #526). Déployés préprod design-system.miklaw.fr. Promotion prod en attente feu vert Mike. Backlog M#43 restant : 9 issues (#505 Epic, #506/#507/#508/#510/#511/#512/#513/#514/#515).
- **2026-06-13 — Audit adversarial complet + remédiation dette + grooming backlog.** Audit 7-axes (8 agents + contre-vérification) → `docs/audit-2026-06-13.md` (161 constats, 42 confirmés/21 nuancés/1 réfuté). **Dette soldée en v2.67.0** : PR #503 (dette) + #504 (entrypoint VERSION) mergées sur `main`, **déployées préprod** design-system.miklaw.fr (sha `99c59fa`, `/version.json`=2.67.0). Milestones **M#37/38/39 fermés** (28 tasks : sync.sh distribue themes/fonts/sprite/brand/Niveau-C ; migration thèmes v2.39 = blocs legacy supprimés de tokens.css + themes.css source unique + 0 token perdu ; CI verify-versions + tests sur PR ; a11y dropdown ; registre kind/pointeurs ; docs). **2 challenges supplémentaires** (taxonomie + complétude) ont révélé des angles morts de l'audit (sidebar 6 liens morts + 32 orphelins, doublons composants, classes fantômes registre) → tout backloggé. Suivis claude-config : #252 (hook branch-aware), #253 (pre-deploy-check lit checkout local).
- **Sprint 36 livré 2026-05-27** — 5/5 PRs mergées (10 SP, 100%, **36e sprint consécutif**). Epic #344 « a11y cleanup résiduel » CLOS. #345 v2.64.6 color-contrast résiduel (14 fixes F1-F14, 458 nœuds, PR #350), #346 v2.64.7 scrollable-region (2 fixes tabindex, 12 nœuds, PR #351), #347 v2.64.8 aria-required-children (1 source initUserMenu, 6 nœuds critical, PR #352), #348 v2.64.9 label résiduel (9 inputs forms, 54 nœuds critical, PR #353), #349 v2.65.0 aria-prohibited-attr donut chart + clôture Epic (1 ligne JS buildDonutChart, 60 nœuds, PR #354). **5 issues séquentielles** (arbitrage Mike up-front S35 reconduit). 15e application pré-allocation versions S17→S36 (v2.64.6/7/8/9/v2.65.0 minor). Mike parti dormir mid-sprint → mode autonome déroulé sans interruption.
- **Bilan a11y final Epic #337+#344** : **141 → 17 violations (-88%)**, **2648 → ~413 nœuds (-84%)**, **60 → 0 critical (-100%)** ✅, 7 → 2 règles. Reste 17 serious uniquement color-contrast dispersé non bloquant. **WCAG AA tenu pour usage productif**. Critère « <10 violations » non atteint (17 > 10) mais critical=0 = objectif principal atteint → dette acceptée (pas de Sprint 37 dédié).
- **v2.65.0 déployée 2026-05-27 12:50 sur design-system.miklaw.fr (Coolify préprod)** — ancien `design-system.msyx.fr` décommissionné 2026-05-22, le nouvel environnement vivant est uniquement Coolify M3 statique (UUID app `zqsyyufvsocsq6xyz36urfc1`, deploy UUID `hzk7v91yr8iypfuei7ojdpds`, deploy_tag `deploy-20260527-124914`, previous `deploy-20260511-000013`). 9 versions cumulées depuis last_deploy v2.56.0 (v2.64.3/4/5/6/7/8/9/v2.65.0). `/version.json` répond `2.65.0 sha=763fd874 built_at=2026-05-27T10:50:21Z`. Validation E2E Playwright OK (CSS + UserMenu + logout). Registry maj : `deploy_mode=static-proxy`, `deploy_target=coolify`, `version=2.65.0`. Rapports audits : `docs/audit-a11y-2026-05-26.md` (post-#345), `docs/audit-a11y-2026-05-27.md` (post-#347), `docs/audit-a11y-2026-05-27-post-349.md` (final).
- **Bonus CI infra (S36 #345)** : 3 workflows GitHub Actions optimisés — cache Playwright (`actions/cache@v4` sur `~/.cache/ms-playwright`) ajouté à visual.yml + a11y.yml + perf.yml (économie 10-15min/run, fix root cause hang download Chrome for Testing) + bump visual timeout 30→45min (120 baselines).
- **Anomalies S36 capit (à hisser claude-config)** : (a) **AI-36.1** subagent /dev #345 timeout stream à 117 tu sans RESULT (récidive AI-35.1, mais cette fois pas dû à `gh pr checks` — vraisemblablement régen baselines VR qui prend > 5min) ; mitigation parent §3d-5 reprise idempotente avec finalisation manuelle (commit + push + PR + lint diacritics). Note : la branche était bien nommée `fix/#345-color-contrast-residuel` → AI-35.2 résolu ; (b) **AI-36.2** subagent /dev #347 a consommé **172 tu** sur scope minimal (3 edits + /review verbeux). Le /review adversarial génère beaucoup de tool_uses inutiles. À hisser : borner /review en budget tool_uses ; (c) **AI-36.3** workflows CI (visual + a11y + perf) tous victimes de `playwright install` hang sur runner GitHub Actions — fixé en S36 via cache mais à hisser dans claude-config templates de workflows (pattern à inclure dès la création d'un nouveau projet) ; (d) **AI-36.4** versionning minor sur dernière PR Epic = bonne pratique cosmétique pour signaler milestone (validé sur v2.65.0 #349) — pattern à documenter pour futurs Epics multi-PRs.

## 🎯 PROCHAINE SESSION — Plan suggéré
**Sprint 1 M#41 LIVRÉ + PUBLIÉ (`@msyx-dev/react` v3.0.0-alpha.8).** Parité **11 ported / 91 pending / 31 n-a**. Tous les autres milestones DS soldés. **Roadmap complète des sprints restants préparée** = [[project-react-parity-roadmap]] (~11 sprints par famille). main=`bb96fa8`, react=**3.0.0-alpha.8**, ds=**2.94.4**.

**PROCHAINE ACTION — au choix (rien de bloquant)** :
1. **Sprint 2 — Formulaires A** (RUNNABLE : 7 issues déjà ticketées) : `/sprint` sur #396 scopé **#452 theme-switcher + #457 dropdown + #463 slider + #464 number-input + #465 search-input + #466 tag-input + #469 file-upload**. Même mécanique que sprint 1 (séquentiel, release alpha.9 en fin). Cf. [[reference-react-port-mechanics]].
2. **Sprint 0 — Cleanup registre** (1 PR DS-CSS, rapide) : reclasser `react:n-a` sur reset-natif/texture-grain/brand-acssi/code-inline/avatar-img/sidebar-link-disabled/sidebar-sublinks (pending 91→84, dénominateur juste). Même recette que #602.
3. **#613** + décision ARIA SegmentedControl (radiogroup canonique ? aligner DS vanilla).

**Règles gravées** (cf. [[reference-react-port-mechanics]] + [[feedback-react-port-verification]]) : séquentiel (0 conflit shared-files) · release consolidée/sprint (bump alpha.N, tag `react-v*`, `[skip-changelog]`) · **tests qui assertent les classes d'ÉTAT** + passe de vérif indépendante AVANT chaque publish (leçon ActionMenu `.open`) · pas de sur-ticketing (le groom de chaque vague crée ses issues + triage proactif-catalogue).

**Décisions Mike en attente (non bloquantes)** : (a) **#613 convention ARIA SegmentedControl** ; (b) **déploiement préprod DS** — `design-system.miklaw.fr` bloqué à 2.94.0 (préexistant ; main servi via `serve_mode:static` git-pull = 2.94.4 ; config Caddy live sans bloc design-system depuis décommission msyx.fr 2026-05-22) → mécanisme de déploiement DS CSS à clarifier.

## Décisions permanentes
- 2026-06-13 : **Run autonome / modif sur ce projet prod = WORKTREE obligatoire** (`.claude/worktrees/`), jamais d'édition en place sur le checkout principal. Le hook N1 `hook-block-protected-writes.py` (matcher Write|Edit|MultiEdit) bloque tout Edit hors `/.claude/worktrees/` ; un run « en place » force les agents à contourner via Bash (garde-fou défait). Cf. memory N4 `feedback-autonomous-prod-worktree`. Memory.md fait exception (basename memory.md exempté).
- 2026-06-13 : **Tout audit catégorisation/cohérence DOIT inclure un check d'intégrité de liens + les 4 surfaces de taxonomie** (registre page/module, sections `<section id>`, modules CSS, NAV hardcodé de nav.js). L'audit 2026-06-13 a manqué 6 liens morts + 32 sections orphelines sidebar faute de croiser le NAV. Détectable en statique. Cf. `feedback-audit-thoroughness`.
- 2026-06-13 : **Séquençage backlog DS = M#43 → M#44 → M#40 → M#41** ; React (M#41) en dernier sur surface gelée. Politique parité React = « gap tracé » (champ registre `react`, suivi auto) tant qu'aucun consumer React ; bascule en lockstep (wrapper obligatoire même PR) dès qu'une app React ship. Mécanisme anti-dérive = issue #523 (à faire en M#43, pas en dernier).
- 2026-05-08 : **Rejet promotion DS de `.input-filter-project` (aksyva)** — détecté par audit consumer S27 #227. Définition : `max-width: 220px;` seul. Project-specific (filtre projet aksyva), pas un pattern réutilisable. Ne PAS proposer de promotion en S28+ sauf demande explicite. Décision tranchée Sprint 28 (préparation).
- 2026-05-08 : Refactor nav.js prévention #206 (Sprint 24 #211) — pattern fragile `+ '<span>...</span>'` éliminé. `const VERSION = '2.X.Y'` extrait en tête de fichier, toutes concaténations HTML remplacées par template literals ES6 (10 zones : `buildHeader`, `buildSidebar`, `renderNotifications`, `updateHeaderUser`, `loadSection`). Refactor strictement isofonctionnel : 0 changement DOM, 0 changement API publique, VR 108/108 PASS sans baseline update. Filet `claude-config#24` (node -c quality-gate) reste actif en complément. **Convention pour tout futur JS DS : pas de concat HTML ligne par ligne — toujours template literals + const extracted pour les constants visibles.**
- 2026-05-08 : Logo officiel MSYX (Sprint 24 #209) — wordmark Monogram v2.42.0 (paths-by-agent S23 #192) **remplacé** par le mark vectorisé du source officiel `msyx.fr/media/logo/logoMSYX.png`. PNG source conservé en `assets/sources/logoMSYX.png` (reproductibilité du tracé via potrace+svgo). Mark-only (pas de wordmark texte), variantes light/dark unifiées sur le même mark. Header ratio 40×40 carré (CSS `layout.css` + `nav.js` ligne 188). Wordmark Monogram historique conservé en `assets/explorations/`. Décision figée : tout futur usage du logo MSYX = ce mark vectorisé, jamais une réinterprétation paths.
- 2026-03-07 : templates de suivi projet = sections statiques mockées (pas de backend), CSS inline
- 2026-03-19 : migration oneshot → projet prod (design-system-project)
- 2026-03-20 : mise à niveau pipeline agentique (board, labels, backlog, docs, auth gate)
- 2026-03-20 : tokens reference = section dans fondation.html#tokens (pas de page séparée)
- 2026-05-01 : convention @ds-version = bump sur 5 fichiers (tokens.css, utilities.css, components.css, layout.css, nav.js) — confirmée Sprint 16, à corriger dans CLAUDE.md §Process point 5
- 2026-05-01 : token `--text-on-accent` thème-aware (Sprint 17 #163) — figure dans tous les blocs `[data-theme]`. Convention sémantique : couleur de texte garantissant lisibilité sur fond `var(--accent)`. Ne pas confondre avec `--accent-light` (variante décorative).
- 2026-05-01 : reset natif global (Sprint 17 #166+#167) — DS impose désormais styles thème-aware sur `<a>` et `:focus-visible` sans classe. Conséquence : tout consumer hérite automatiquement, plus de duplication CSS côté projets. Cascade garantie sur 11 composants existants à focus custom (`.btn-*`, `.input`, `.accordion-header`, `.dropdown-option`, `.fab`, `.hub-card`, etc.).
- 2026-05-01 : pré-allocation des versions par le parent /sprint pour sprints multi-bumps (>2 issues touchant @ds-version) — parent injecte `ta version cible : v2.X.Y` dans chaque prompt /dev. Validé Sprint 17 (0 conflit git vs 2 attendus en S16). 7e application consécutive S17→S24 (S24 : v2.43.0 #209 + v2.43.1 #211 sur même fichier nav.js, 0 conflit). Convention obligatoire pour CLAUDE.md.
- 2026-05-01 : pattern "absorption d'issue" (Sprint 17 #167 absorbed par #166) — conditions : même fichier CSS, même cascade, ACs proches, sprint commun. Procédure : justifier dans /groom de l'issue absorbante, inclure ACs absorbed dans la spec, mentionner `closes #X, closes #Y` dans PR title pour fermeture auto.
- 2026-05-06 : pattern "codemod idempotent + aliases legacy bidirectionnels" pour refactor de tokens (Sprint 20 #186) — sed -E word-boundaries (`(^|[^a-zA-Z0-9_-])--TOKEN([^a-zA-Z0-9_-]|$)`) + aliases dans `tokens.css` (`--old: var(--new); /* deprecated, remove in vN */`). Garantit 0 régression rendu côté consumers. Run 1× = N modifs / run 2× = 0 modif. Réutilisable pour tout futur rename/split de token.
- 2026-05-06 : architecture CSS split en 25 modules + barrel (Sprint 21 #188) — `shared/css/components.css` devient un barrel pur < 1.5 KB qui `@import` 25 fichiers de `shared/css/components/`. Ordre des `@import` reproduit la cascade originale ligne par ligne (vérifié mécaniquement par diff). `components-core.css` = 7 modules essentiels (~42 KB) pour consumers légers. `sync.sh --components=core|<liste>` pour copie sélective (tree-shake manuel). Aucun changement de rendu (VR PASS 18/18 sans baseline update).
- 2026-05-06 : type modular scale ratio 1.25 (Sprint 21 #189) — 8 tokens `--type-*` (échelle Major Third, `--type-12: 0.75rem` à `--type-49: 3.052rem`) dans `tokens.css`. 4 tokens `--lh-*` préexistants conservés. Classes `.typo-*` (10) et `.text-*` (5) mappées sur `--type-*` (sauf `.typo-display` 3.5rem et `.text-lg` 1.125rem conservés en literal). Section `#type-pairing` dans `pages/fondation.html` (4 pairings canoniques : Hero, Section, Card, Data). Règles pairing dans `SKILL.md` pour cohérence agents. Diffs VR sub-pixel absorbés par tolerance Playwright.
- 2026-05-06 : VR matrice complète 108 baselines (Sprint 22 #191) — 3 thèmes × 2 modes × 9 pages × 2 viewports (1280 + 375). 12 projects Playwright `<theme>-<mode>-<viewport>` dans `playwright.config.ts`. Naming baseline : `visual-tests/baseline/<theme>-<mode>-<viewport>/<page>.png`. Timeout CI 30 min (vs 15). Volume binaire ~5-8 MB. Filet de protection pour theme generator + tout refactor CSS large. Toute extension future (10e page) → 120 baselines (linéaire prévisible).
- 2026-05-06 : Theme generator JSON → CSS (Sprint 22 #190) — `themes/*.json` source de vérité unique, `shared/build-themes.js` compilateur Node no-deps déterministe (tri alphabétique strict des propriétés CSS), `shared/scaffold-theme.sh <name>` helper DX. Architecture : msyx implicite dans `:root` de tokens.css, acssi+nhood seuls dans `themes.css` généré (importé après tokens.css dans styles.css). Blocs CSS legacy commentés dans tokens.css avec marqueur `MIGRATED → themes/*.json — to remove in v2.40.0`. Byte-identité validée par VR PASS 108/108 sans baseline update. Ouvre le chemin pour les consumers qui veulent dériver leurs propres thèmes.
- 2026-05-07 : Brand identity officielle (Sprint 23 #192) — wordmark Monogram inspiré du logo historique MSYX (https://www.msyx.fr/media/logo/logoMSYX.png). 4 fichiers SVG dans `assets/` : `logo-msyx.svg` (PRIMARY 200×60), `logo-msyx-mark.svg` (mark seule 60×60, favicon-ready), `logo-msyx-light.svg` (fond clair), `logo-msyx-dark.svg` (fond sombre). Mark : blob organique + M en espace négatif double sommet via `fill-rule="evenodd"`. Gradient vertical `<linearGradient>` : `#10b981` turquoise → `#3b82f6` bleu → `#8b5cf6` violet. ≤ 1 path complexe par variante. `role="img"` + `<title>` (a11y). Migration : header (`nav.js` `buildHeader()` → `<img src="/assets/logo-msyx.svg">` 120×32), index.html, canonical-pages/login.html. 2 explorations Monogram dans `assets/explorations/` (historique). **Décision révisée S24 #209 : remplacé par le mark vectorisé officiel — voir 2026-05-08.**
- 2026-05-07 : Signature spatiale (Sprint 23 #192) — `shared/css/components/signature.css` (26e module CSS). Gradient underline 2px via `::after` sur `.main .section-header .overline`. Accent visuel 32×2px, `var(--gradient-1)`, `border-radius: 2px`. Rend le DS reconnaissable même en grayscale/impression mono. Importé dans `components.css` après `_base.css`. **Aucune baseline VR mutée** (32×2px sub-pixel + faible contraste = sous tolérance Playwright).
- 2026-05-07 : Token `--texture-grain` (Sprint 23 #192) — formalise `--noise-texture` (ticket #12 absorbé). `--texture-grain: url("data:image/svg+xml;utf8,<svg>feTurbulence noise 0.9Hz 4 octaves stitch</svg>")` + `--texture-grain-opacity: 0.015` dans `tokens.css`. `body::after` dans `styles.css` refactorisé pour utiliser ces tokens. Section « Texture grain » documentée dans `pages/fondation.html`.
- 2026-05-07 : Modal focus restore WAI APG (Sprint 23 #174) — helper privé `attachFocusRestore(dialog)` intégré directement dans `shared/components.js` (PAS de fichier séparé `modal-focus.js` — décision figée en spec : `sync.sh` ne distribue que CSS, donc helper JS doit vivre dans `components.js`). Pattern WAI APG : capture `document.activeElement` au `showModal()`, restaure `_trigger.focus()` au `close`. Couvre 4 voies de fermeture (ESC, backdrop, `[data-modal-close]`, `dialog.close()` programmatique). Idempotent via flag `__focusRestoreAttached`. Edge cases : trigger null ou supprimé du DOM → skip silencieux. Câblage automatique dans `initModals()` (statiques) + `window.__openModal` (dynamique). Promotion du pattern aksy UC-288 v0.7.10. Corrige WCAG 2.4.3 + 2.4.7 sur toutes les modales DS et tous les consumers.
- 2026-05-07 : Test E2E DS — pattern `page.route()` pour `serve -s` (Sprint 23 #204 fix CI flaky) — `serve` v14 avec flag `-s` (SPA mode) redirect 301 sur `.html` (clean URL) → SPA fallback. Tout test E2E qui visite `/pages/*.html` doit intercepter via `page.route("**/pages/feedback.html", route => route.fulfill({ path: "pages/feedback.html" }))` côté Playwright. Sinon → atterrit sur la page de login auth gate. **Documenté dans `SKILL.md` S24 #212.**
- 2026-05-07 : Pattern « arbitrage upfront pour off-keyboard creative » (Sprint 23 #192) — pour tout sprint avec contenu créatif (wordmark, signature, palette, etc.), Mike tranche en début de session via 1 round `AskUserQuestion` structurée (avec previews ASCII et références visuelles via Read tool image si pertinent). Décisions injectées dans le prompt /spec et gravées dans la spec textuelle (commentaire GitHub). Le subagent /dev livre ensuite en autonomie totale sans interruption synchrone. Validé S23 #192 (8 SP créatif livrés sans round synchrone). Économise les rounds Mike.
- 2026-05-07 : **Incident #206 — SyntaxError JS a traversé 6 filets pipeline.** Cause : `nav.js` ligne 189, quote fermante oubliée par PR #205 lors du bump version. Effet : site DS prod totalement cassé (toutes pages, header/sidebar/logos absents) pendant 6h. Détection manuelle par Mike. **Règle dérivée : tout projet sans bundler (HTML/CSS/JS pur) doit avoir `node -c` au quality-gate** + smoke test DOM headless post-deploy avec rollback auto. Filet `claude-config#24` CLOSED 2026-05-07. **Cause structurelle éliminée S24 #211** (extract VERSION + template literals). Pattern fragile à éviter : `+ '<span>...</span>'` ligne par ligne — toujours `const VERSION` extracted + template-literals.
- 2026-05-08 : **Variable CSS `--chevron-select` theme-aware (Sprint 25 #217)** — pattern data:uri encodé dans `tokens.css` avec 6 déclinaisons (`:root`, `[data-mode="light"]`, et 4 `[data-theme]/[data-mode]`). Couleur du chevron `<select class="input">` miroir de `--text-dim`, plus de hardcoded `#94a3b8`. Pattern réutilisable pour tout autre data:uri dans les tokens (icons inlined, backgrounds décoratifs). Convention : tout SVG inliné en data:uri qui doit s'adapter au thème → variable CSS dans `tokens.css` avec déclinaisons par `[data-theme]/[data-mode]`.
- 2026-05-08 : **Pattern transition canonique (Sprint 25 #218)** — `transition: all` est interdit. Remplacement systématique par propriétés ciblées : `transition: transform 0.4s var(--ease-standard), box-shadow 0.4s var(--ease-standard), border-color 0.4s var(--ease-standard);`. Élimine les flicker compositing layer thrashing (Safari mobile + Chrome). `will-change: transform` strict sur 2 sélecteurs uniquement (`.card`, `.hub-card` — les seuls combinant `backdrop-filter` + `transform:hover`). Pas d'usage systématique de `will-change` (épuise GPU memory). 30 occurrences corrigées sur 13 modules CSS S25.
- 2026-05-08 : **Tokens space `--space-2: 0.75rem` + `--space-5: 1.25rem` (Sprint 25 #216)** — ajoutés à l'échelle modulaire pour éliminer les hardcodés 0.7rem (×3+), 0.9rem, 1.2rem dans 7 modules CSS. Décision tranchée en spec : créer 2 nouveaux tokens plutôt qu'aligner sur les tokens existants (préserve clic targets a11y et hiérarchie visuelle). Échelle finale : `--space-1: 0.25rem`, `--space-2: 0.75rem`, `--space-3: 1rem`, `--space-4: 1.5rem`, `--space-5: 1.25rem`, `--space-6: 2rem` etc. (rétro-compat conservée). Documenter dans `pages/fondation.html#tokens` au prochain sprint design.
- 2026-05-08 : **Restructure composants × pages (Sprint 25 #219)** — séparation sémantique entre pages thématiques actée et figée. Convention : `composants.html` = atomic UI (cards, buttons, badges, modals, etc.) ; `feedback.html` = patterns feedback (toasts, modals, drawer, FAB, **+pagination, +tooltip, +comments**) ; `navigation.html` = navigation primaire ET secondaire (header, tabs, breadcrumbs, **+action-menu**) ; `templates.html` = templates de page complets (kanban, roadmap, **+pricing**) ; `fondation.html` = système de design (couleurs, typo, theming, **+theme-switcher**) ; `formulaires.html` = inputs de formulaire (le CSS login-form aussi y vit maintenant via `forms.css`, plus dans `templates.css`). Tout futur composant doit suivre cette taxonomie. `components-registry.json` est la source de vérité agent (synchroniser obligatoirement à chaque ajout/déplacement).
- 2026-05-08 : **Capitalisation pattern « warning explicite anomalie récente dans le prompt /dev »** (Sprint 25 mid-sprint). Quand un subagent timeout sans RESULT (#217 puis #218), le parent injecte dans le prompt /dev des subagents suivants une mention explicite des anomalies récentes avec budget tool_uses précis (« À 70 tool_uses : être prêt à pousser. À 90 tool_uses : sortie IMPÉRATIVE avec ligne RESULT »). Validé sur #216 (62 tool_uses) et #219 (89 tool_uses) — zéro timeout après warning. Pattern à hisser dans le prompt /dev N1 (claude-config) — action item AI-25.2.
- 2026-05-08 : **Hook anti-merge bug inter-projet identifié (Sprint 25 #218)** — `hook-block-merge-with-subagent.py` lit `agents-active.json` mais ignore le champ `project`. Conséquence : un coder actif sur `aksyva-project` bloque `gh pr merge` sur `design-system-project`. Faux positif observé S25 (PR #222 bloquée ~3h). Fix : comparer le `project` de chaque subagent actif avec le repo cible du `gh pr merge` (extraire via `gh pr view --repo` ou cwd → repo → projet). Action item AI-25.1 (P1) pour repo claude-config.
- 2026-05-09 : **Filet anti-régression CI à 4 workflows (Sprint 30)** — `ci.yml` (lint HTML + check-diacritics + perf-budget gzip warn-only, < 30 s), `visual.yml` (108 baselines Playwright VR, ~20 min coût structurel), `perf.yml` (`@lhci/cli` Lighthouse 6 runs multi-thèmes via query params `?theme=X&mode=Y`, assertions LCP/TBT/CLS/score warn-only, ~2 min), `a11y.yml` (`@axe-core/playwright` 54 runs WCAG 2.1 AA dry-run, `continue-on-error: true`, ~2 min). Référentiel : `docs/PERF-BUDGET.md`. **Convention** : tout nouveau filet CI = workflow séparé (pas de greffe sur `ci.yml` monolithique) pour timeouts indépendants, clarté des logs et autonomie de l'artifact upload. Mode `warn-only` par défaut sur les filets perf/a11y (évite faux positifs, passe en block-mode après 2+ sprints stables).
- 2026-05-10 : **Pattern « rebase chain » pour sprints multi-PR séquentiels (Sprint 31)** — sur sprint avec >= 5 PRs touchant les mêmes fichiers de version (typique sprint Bugfix : 9 PRs × bump @ds-version), chaîne de rebases `--theirs` à exécuter par le parent /sprint après chaque merge. Liste fichiers à --theirs étendue de 6 à 7 : `package.json`, `shared/css/tokens.css`, `shared/css/utilities.css`, `shared/css/components.css`, `shared/css/layout.css`, `shared/nav.js`, **`shared/components-registry.json`** (ajout S31, conflit non détecté précédemment car sprints précédents ne touchaient pas tous le registry). Procédure : (1) merge PR n, (2) `git pull --ff-only`, (3) pour chaque PR pending : checkout, `git rebase main`, boucle `git checkout --theirs` sur les 7 fichiers, `git add`, `git rebase --continue`, `git push --force-with-lease`. Fonction bash `rebase_pr()` réutilisable. 8 rebases successifs S31 = 0 conflit non-version. CI re-tourne 1× par rebase (~22 min visual). Coût total CI Sprint Bugfix 9 PRs = ~3h cumulé mais parallélisable (groom+spec en // dès le départ).
- 2026-05-10 : **Validation visuelle Mike OBLIGATOIRE pour assets custom (Sprint 31 hotfix #247)** — Pour tout asset visuel produit par un subagent /dev (logo, illustration, icône brand, gradient, palette custom), le subagent NE DOIT PAS livrer en autonomie totale sur la base d'une description textuelle. Cas observé S31 #247 : le subagent /dev a "tracé manuellement" un path SVG `M en double-pic` avec coordonnées arbitraires (`M220 1100 L430 470 L630 880...`) totalement déconnecté du logo officiel — résultat dégradé en prod, refusé par Mike. **Procédure obligatoire** : (a) le subagent /dev produit une preview locale (screenshot, ASCII, ou commit sur branche dédiée NON pushée) ; (b) le parent /sprint déclenche un `AskUserQuestion` avec preview (Read tool image si SVG/PNG existant en référence) ; (c) Mike valide ou redirige avant push. Le pattern « arbitrage upfront S23 #192 » s'étend désormais aussi aux assets vectoriels custom, pas seulement aux choix de palette/wordmark. **Hotfix correctif S31 #267** : swap vers PNG officiel direct (`<img src="/assets/sources/logoMSYX.png">`, 96 KB) le temps qu'un vrai SVG fidèle soit produit en S32+ avec validation Mike.
- 2026-05-11 : **Pattern rebase chain S31 — limite identifiée (Sprint 32)** — `--theirs` aveugle sur les 7 fichiers fonctionne bien pour sprints "bump-only" (S31 9 PRs bugfix mineurs où les modifs sont quasi-disjointes) mais **écrase les modifs sur fichiers logique cross-PR**. Cas observé S32 : 3 PRs (#265 mode-switch + #266 polish + #268 brand) touchent toutes `nav.js` + `layout.css` + `components-registry.json` dans des zones différentes — `--theirs` aveugle a écrasé les modifs intermédiaires (mode-switch perdu de nav.js, styles `.mode-switch` perdus de layout.css, classes mode-switch supprimées du registry + entrées brand-* perdues). Mitigation parent S32 : ~15 min merge intelligent fichier par fichier (récupérer la version main + ré-appliquer les changements minimaux de la branche en cours). **Capit à hisser claude-config (AI-32.1)** : la sortie /dev doit déclarer quels fichiers sont "version pure" (bump seul → --theirs OK) vs "logique cross-PR" (modifs croisées → merge manuel ou cherry-pick par hunk). Sinon le parent applique un merge intelligent pour ces derniers.
- 2026-05-11 : **Pattern arbitrage upfront étendu issues brand textuelles (Sprint 32 #268)** — 3e application consécutive après S23 #192 (wordmark SVG) et S31 #247 (logo SVG). Pour #268 (wordmark texte + mark DS + section docs), 1 round `AskUserQuestion` structurée avec previews ASCII a permis de figer 4 décisions visuelles (sketches header + login, choix module CSS, comportement mobile) sans interruption pendant /dev. Décisions injectées en commentaire d'arbitrage GitHub sur l'issue (URL : `#issuecomment-XXX`) avant le spawn /dev. Le subagent a livré conforme sans re-arbitrer (120 tool_uses, RESULT propre). **Pattern : tout sprint avec une issue brand textuelle (wordmark, mark, signature, gradient charte) → arbitrage upfront Mike obligatoire**, même si l'asset n'est "que" du texte CSS.

## Historique sessions
- 2026-05-27 12:50 — **Deploy v2.65.0 sur Coolify préprod design-system.miklaw.fr** : 9 versions cumulées depuis v2.56.0 livrées en une seule passe via `~/.claude/scripts/m3/deploy-coolify-static.sh design-system`. Registry corrigé en amont (champs `deploy_mode`/`deploy_target` null → `static-proxy`/`coolify` ; `version` 2.56.0 → 2.65.0). Pre-deploy check OK (1 warn INFRASTRUCTURE.md absent, non bloquant). Audit logging OK (0 anomalie). Audit sécu pre-deploy (security-auditor Opus) **VERDICT OK** : 0 CRITICAL/HIGH, 1 INFO `.dockerignore` n'exclut pas `.env` (défense en profondeur, pas de `.env` présent). Coolify deploy en 6 polls (~90s, status=finished). Validation E2E : healthchecks `/health.json` `/version.json` 200, chain edge→Traefik X-Forwarded-Proto OK, auth gate 302 vers Authentik, E2E Playwright recovery token akadmin OK (CSS + UserMenu + logout). **Friction (1 faux positif lint à hisser claude-config)** : `caddy-snippet-lint.sh design-system` retourne 4 erreurs car cherche `/etc/caddy/Caddyfile.d/miklaw.caddy` alors que le snippet réel est `00-miklaw.caddy` (préfixe `00-` pour ordre Caddy include) — `sudo diff` entre VPS et `~/projects/prod/coolify/Caddyfile.d/00-miklaw.caddy` confirme **NO DRIFT**. Bug du script de lint, pas du déploiement. Tag git `v2.65.0` + GitHub Release déjà créés ce matin 07:01 UTC (session précédente). Tag rollback `deploy-20260527-124914` créé localement et pushé. Registry _global commité+pushé (commit `f27b4b5`). DS = Coolify M3 statique (image `caddy:2-alpine`), build automatique au git push (Coolify suit le webhook). Cumul depuis last_deploy v2.56.0 : 9 versions (v2.64.3 → v2.65.0) — clôture Epic #344 a11y WCAG AA + recalibrage tokens couleur cumulés Lot 2 S35 + Sub-A S36.
- 2026-05-08 — **Sprint 25 terminé** : 4 PRs DS séquentielles sur `forms.css` (épicentre conflit anticipé) — #221 v2.44.0 chevron theme-aware via `--chevron-select` + sprite Lucide cleanup + emojis context-menu → Lucide closes #217 ; #222 v2.45.0 transitions ciblées (30 occurrences `transition: all` éliminées sur 13 modules CSS) + will-change strict cards closes #218 ; #223 v2.46.0 tokenisation `--space-*` (2 nouveaux tokens `--space-2: 0.75rem` + `--space-5: 1.25rem`, 12 sélecteurs M-01→M-12 retokenisés) closes #216 ; #224 v2.47.0 restructure composants × pages (7 sections déplacées R-01 à R-07, components-registry.json resync 4 entrées ajoutées + 5 page descriptions CLAUDE.md) closes #219. **12/12 SP = 100% velocity, 25e sprint consécutif.** Audit Phase 1 (#210) entièrement remédié (40 findings → 4 sous-issues fermées). Pré-allocation versions 8e application consécutive S17→S25 (4 versions séquentielles modifiant les mêmes 5 fichiers @ds-version, 0 conflit). Arbitrage upfront round R-03/R-04/R-07 validé par Mike pour #219 (3e application du pattern S23). Anomalies S25 : (a) subagents #217 + #218 ont timeout à 100+ tool_uses sans ligne RESULT — récidive 2x — mitigation parent §3d-5b (~10 min/issue : push branche locale propre + create PR manuel), capitalisation appliquée mid-sprint dans le prompt /dev (warning explicite des budgets) → #216 et #219 RESULT propre 62/89 tool_uses ; (b) **hook anti-merge bloque pour subagent actif sur AUTRE projet** (aksyva coder + devops-deployer) — faux positif inter-projet a bloqué #218 pendant ~3h (action item AI-25.1 P1 pour claude-config) ; (c) CI lint check-diacritics fail sur RELEASES.md #218 (`regression`/`themes` sans accent) — fix parent + capitalisation prompt /dev. Bilan §4 publié : retro `docs/retros/sprint-25.md`, velocity.json maj (S25=100%), CLAUDE.md (N2) descriptions pages mises à jour par subagent #219, ARCHITECTURE.md version courante v2.47.0, milestone GH #26 fermé.
- 2026-05-08 — **Sprint 26 terminé (mini-sprint reliquat audit)** : 1 PR DS — #226 v2.48.0 tokenisation icon/avatar sizes closes #225 (P-04 à P-08, 8 nouveaux tokens : 5 `--avatar-size-*` + 3 `--card-icon-*`, 11 sélecteurs migrés dont 1 bonus `.hub-card-icon` détecté en groom). **2/2 SP = 100% velocity, 26e sprint consécutif.** **Audit Phase 1 (#210) 100% clôturé** (40/40 findings remédiés via 5 sous-issues #216-#219 + #225). Pré-allocation versions 9e application consécutive S17→S26 (0 conflit). 0 anomalie subagent (RESULT propre en 57 tool_uses, capitalisation S25 « warning explicite anomalie récente » 3e application efficace). 0 friction CI (check-diacritics OK, capitalisation S25 efficace). Bilan §4 publié : retro `docs/retros/sprint-26.md`, velocity.json maj (S26=100%), ARCHITECTURE.md version courante v2.48.0, milestone GH #27 fermé.
- 2026-05-08 21:03 — **Sprint 25+26 déployés (cumul)** : v2.48.0 en prod sur design-system.msyx.fr (deploy_tag `deploy-20260508-210320`, previous `deploy-20260508-145548`), registry.json maj (commit `f3c6aa4` dans `~/projects/_global/`), gh release v2.48.0 publiée (https://github.com/msyx-dev/design-system-project/releases/tag/v2.48.0), package.json bumpé 2.43.1 → 2.48.0 (commit `e75dc70`), .gitignore enrichi `shared/dist/` + `pnpm-lock.yaml` (commit `d6f8ba0`). Audit sécu pre-deploy : VERDICT OK (0 finding bloquant ; 1 ATTENTION non bloquant traité = `shared/dist/` ajouté au .gitignore avant deploy ; 2 INFO non bloquants : `onclick` inline pages/feedback.html démos, `bodyHTML` opt-in components.js documenté pour devs consumers). Smoke tests OK (302 redirect login sur /, 200 sur tokens.css). Healthcheck post-deploy 1/1 OK, pas de rollback. Cumule 5 bumps mineurs depuis prod v2.43.1 du 14:57 (v2.44.0 chevron theme-aware → v2.45.0 transitions ciblées → v2.46.0 tokenisation `--space-*` → v2.47.0 restructure composants × pages → v2.48.0 tokenisation icon/avatar). **Audit Phase 1 (#210) 100% clôturé en prod** : 40/40 findings remédiés et déployés.
- 2026-05-08 — **Sprint 27+28 livrés + S28 déployé** : S27 coordination « no-PR » 4/4 SP — #227 audit consumers post-v2.48.0 PASS-AVEC-DETTE (drift aksyva -25/acssi -35/aksy -13 minor, 0 régression DS) + #228 notify aksy 4 DS-EXCEPTIONs S23 (4 commentaires postés sur aksy#265 #278 #301 #254). S28 promotions DS 3/3 SP — 2 PRs parallèles : #231 fix `components-registry.json` `reset-natif` cssClasses null→[] + check-components.sh defensive Python (closes #229), #232 v2.49.0 `.card-link` cards.css + `.badge-nav` badges.css + `.toast-message` alerts.css (closes #230, classes promues depuis aksyva+acssi overrides). Friction S28 : conflit RELEASES.md + components-registry.json entre #231 et #232 (résolu manuellement parent ~5 min) + hook anti-merge faux positif inter-projet (devops-deployer aksyva PID 678365, ~10 min attente). 28e sprint consécutif à 100%. **S28 déployé 22:52** : v2.49.0 prod (deploy_tag `deploy-20260508-225244`, previous `deploy-20260508-210320`), gh release v2.49.0 publiée, registry.json maj (commit `2663d6b`, fix manuel après devops-deployer ayant inscrit faux tag). Audit sécu VERDICT OK (0 finding sur diff additif CSS pur). Healthcheck OK, pas de rollback. 3 issues consumers ouvertes pour suivi : aksyva#117, acssi-core#523, aksy#456.
- 2026-05-09 — **Sprint 29 livré + déployé** : 2 PRs DS — #235 v2.49.1 fix #233 tokens.css ACSSI (suppression bloc legacy commenté L244-616 = 373 lignes mortes, débloque resync aksy#456) ; #236 v2.50.0 refactor #234 `.code-inline` vers tokens canoniques (`--font-mono`, `--surface-solid`, `--border-color`, `--radius-xs`) + démo dédiée pages/divers.html + entrée components-registry.json (promotion DS-EXCEPTION aksy UC-373). **4/4 SP = 100% velocity, 29e sprint consécutif.** Pré-allocation versions 10e application consécutive S17→S29 (#235 v2.49.1 + #236 v2.50.0 séquentiels). Friction : conflit git attendu sur les 5 fichiers @ds-version + RELEASES.md entre #235 et #236 (parallèles) → rebase manuel parent (~3 min, pris theirs sur version files, merge manuel RELEASES.md avec ordre v2.50.0 puis v2.49.1). 0 anomalie subagent (RESULT propres : 38 + 51 tool_uses). Spec #234 a découvert que `.code-inline` existait déjà dans `interactive.css` L11 (~30 usages réels, 97 estimés post-grep) → l'issue est devenue refactor au lieu de création. Milestone GH #30 fermée auto par post-merge.sh. **Déployé 10:13 UTC** : v2.50.0 prod (deploy_tag `deploy-20260509-101351`, previous `deploy-20260508-225244`), gh release v2.50.0 publiée, registry.json maj (commit `7b68ecf`), package.json bumpé 2.49.0 → 2.50.0 (commit `d6e28a1`). Audit sécu pre-deploy : VERDICT OK (0 finding, diff 100% CSS additif/cleanup). Smoke tests OK.
- 2026-05-09 — **Sprint 30 livré + déployé** : 4 PRs DS — #243 v2.51.0 perf-budget gzip + step CI warn-only (#239 #237-A) ; #244 v2.52.0 axe-core dry-run a11y `@axe-core/playwright` + workflow `a11y.yml` séparé + audit `docs/audit-a11y-2026-05-09.md` **0 violations sur 54 runs** (#242 #238-infra) ; #245 v2.53.0 Lighthouse CI baseline 1 page MSYX dark `@lhci/cli` + `lighthouserc.cjs` + workflow `perf.yml` **score 1.0** (#240 #237-B) ; #246 v2.54.0 Lighthouse multi-thèmes 6 runs (mécanisme query params `?theme=X&mode=Y` + script anti-FOUC inline `composants.html` → localStorage) tous score 1.0 + `docs/PERF-BUDGET.md` complet + `CONSUMER_GUIDE.md` Tree-shake guide actualisé (#241 #237-C). **4/4 SP = 100% velocity, 30e sprint consécutif.** Sprint déclenché par challenge fortuit du dossier `docs/claude_design/` (backlog caduc créé le matin même post-S29, supprimé en commit `0352417`). 3 outils perf CI désormais opérationnels en complément de la VR Playwright (108 baselines). `#238-fix` non créé (0 violation = pas de dette, le DS passait déjà axe sans fix grâce à l'effort manuel cumulé : Audit Phase 1, contrastes v2.31.0, focus restore v2.41.0, reset natif, disabled global). Friction S30 : (a) orchestrator async lancé pendant promenade Mike a échoué partiellement (s'est arrêté en plein commit #239) → travail récupéré du worktree avant cleanup, livré en mode séquentiel manuel au retour ; (b) hook `block-merge-with-subagent` friction systématique (~6 manuels `agent-tracker.py stop` cumulés sur le sprint, agents finis non auto-retirés du tracker) ; (c) hook `block-coder-mutations` correctement bloque RELEASES.md pendant /dev (consolidation parent post-merge respectée) ; (d) hook `block-destructive` faux positif sur `rm -f "$STATE_DIR/.state-242.json"` lu raw avant expansion. Visual VR Playwright = ~20 min × 4 PRs = 80 min cumulés CI (coût structurel filet anti-régression, accepté). Pré-allocation versions 11e application consécutive S17→S30 (4 versions séquentielles v2.51-v2.54, 0 conflit). **Déployé 20:30** : v2.54.0 prod (deploy_tag `deploy-20260509-203016`, previous `deploy-20260509-101351`), 4 versions cumulées depuis v2.50.0. DS = static Caddy file_server, déploiement = git pull. Bilan §4 publié : retro `docs/retros/sprint-30.md` (3 actions S31 : agent-tracker auto-cleanup, re-évaluer /sprint async, doc durée VR), velocity.json maj S30=100%, milestone GH #31 fermé.
- 2026-05-10 08:54 — **Sprint 31 livré + déployé (Bugfix UX)** : 9 PRs DS toutes mergées séquentiellement v2.54.1 → v2.54.9 sur 9 bugs UI/régressions remontés en revue Mike 2026-05-09 — **#247 v2.54.1** logo MSYX M en pic restauré (régression PR #213/S24 #209) + test non-régression ; **#251 v2.54.2** ACSSI light mode (race init `updateModeButtons` vs anti-FOUC, fix défensif nav.js) ; **#248 v2.54.3** sidebar bleed-through (backdrop-filter sur `.sidebar-filter-wrap`) ; **#250 v2.54.4** code-block overflow-wrap anywhere (col 3 grid débordement URL longue) ; **#252 v2.54.5** mise en page 4 sections showcase (régression structurelle HTML : section badges fermée trop tôt + 3 wrappers `.demo-grid` manquants) ; **#253 v2.54.6** polish .btn-danger/.btn-success/.btn-warning (alignement glassmorphism .btn-primary, refactor sélecteur partagé) ; **#249 v2.54.7** section theming refactorée (classes orphelines + palettes ACSSI/Nhood inline → `.color-grid--compact`) ; **#255 v2.54.8** toasts positioning + animation + a11y (top calc(--header-h), stack-shift, ease-spring, mobile, role dynamique) ; **#254 v2.54.9** icône notification Lucide i-bell (swap emoji 🔔 OS-dependent). **9/9 PRs = 16/16 SP = 100% velocity, 31e sprint consécutif.** Workflow §2a parallèle agressif (4 vagues, jusqu'à 4 subagents simultanés en groom+spec — sans risque car spec = commentaire GitHub uniquement). §3 dev+review en 5 lots × 2 //. Pré-allocation versions 12e application consécutive S17→S31 (9 versions séquentielles v2.54.1→v2.54.9, 0 conflit avec subagents — tous conflits gérés par parent en rebase chain). **Pattern « rebase chain » formalisé S31** (voir décision permanente 2026-05-10) : ajout `shared/components-registry.json` à la liste --theirs (passé de 6 à 7 fichiers). 8 rebases successifs sans erreur. Friction : (a) chaîne de 9 CI re-runs visual ~20min × 9 = ~3h cumulé (coût structurel mais parallélisable côté GitHub Actions) ; (b) 0 anomalie subagent (RESULT propre sur 9/9, budget tool_uses ≤ 79 max sur #249) ; (c) 0 hook block-merge faux positif (peut-être grâce à cleanup S30). Aucune issue absorbée. Milestone GH #32 fermé auto par post-merge.sh. **Déployé 08:54** : v2.54.9 prod sur design-system.msyx.fr (deploy_tag `deploy-20260510-085350`, previous `deploy-20260509-101351`), gh release v2.54.9 publiée (https://github.com/msyx-dev/design-system-project/releases/tag/v2.54.9), registry.json maj (commit `9ca11ce` dans `~/projects/_global/`). Audit sécu pre-deploy : VERDICT OK (0 CRITICAL, 0 HIGH, 1 INFO pré-existant hors scope `pages/data.html` table grid `tr.innerHTML` data statique). Smoke tests OK (302 auth gate sur /, /site.html, /pages/composants.html, /shared/css/tokens.css, /assets/logo-msyx.svg, /shared/icons/sprite.svg). Caddy actif. DS = static file_server, déploiement = git pull (déjà sync local = serveur).
- 2026-05-10 20:43 — **Hotfix #247 logo PNG direct (v2.54.10)** : Mike a refusé en revue post-deploy S31 le tracé SVG manuel ajouté par le subagent /dev en S31 ("tu as totalement transformé mon logo" — coordonnées arbitraires zigzag horizontal sans rapport avec le logo officiel W+M). PR #267 : swap des 3 références logo (`shared/nav.js`, `index.html`, `canonical-pages/login.html`) vers `<img src="/assets/sources/logoMSYX.png">` directement (96 KB, conservé dans le repo depuis S23). Les 4 SVG `assets/logo-msyx*.svg` restaurés à leur état pré-S31 (path bricolé `#ffffff` retiré). Test `tests/regression/logo-msyx-paths.test.js` supprimé (validait le bricolage). CI all-green incluant visual VR (diff sub-pixel sous tolérance Playwright — sans doute parce que le logo header ne représente que 40×40px sur les baselines 1280px). Bump v2.54.9 → v2.54.10. Déployé sur design-system.msyx.fr (deploy_tag `deploy-20260510-204339`, previous `deploy-20260510-085350`), gh release v2.54.10 publiée, registry.json maj (commit `ea8852f`). Smoke test prod OK (302 auth gate sur /, /site.html, /assets/sources/logoMSYX.png). **Leçon** : pour tout asset visuel custom (logo, illustration), demander à Mike validation visuelle avant push — un subagent /dev qui "trace manuellement" sur la base d'une description textuelle peut produire un résultat très éloigné de l'intention. Issue #247 follow-up à créer en S32 : vrai SVG fidèle (2 paths W vert + M bleu en gradients) pour gagner ~95 KB. 2 issues post-revue Mike créées : #265 switch dark/light intuitif, #266 boutons danger/success/warning theme-aware.
- 2026-05-27 — **Sprint 36 livré (a11y cleanup résiduel — clôture Epic #344)** : 5 PRs DS séquentielles — **#350 (#345) v2.64.6** color-contrast résiduel (14 fixes F1-F14 selon spec : `--text-dim` msyx-dark, `--code-string`/`--code-number` light, `--*-light` ACSSI+Nhood light cascade, `--deco-cyan` light, .access-denied-code danger→danger-light, .input-error-msg/.login-error, .dtree-choice accent→accent-light, .fab-extended WCAG 1.4.11, theme-card--future/cal-day.other-month opacity, retrait styles inline + hex avatars → tokens — 458 nœuds résorbés, baselines VR option A 60 baselines régénérées localement et commit) ; **#351 (#346) v2.64.7** scrollable-region (tabindex="0" sur .bottom-sheet-content + centralisation initCopyButtons sur .code-block, 12 nœuds) ; **#352 (#347) v2.64.8** aria-required-children CRITICAL (1 source unique initUserMenu : role=presentation + aria-hidden header + button hors form via form="formId", 6 nœuds × 6 occurrences global header) ; **#353 (#348) v2.64.9** label résiduel CRITICAL (9 inputs formulaires.html : 7× for/id + 2× aria-label option B, 54 nœuds) ; **#354 (#349) v2.65.0** aria-prohibited-attr (1 ligne JS buildDonutChart role="img" miroir Lot 1 buildPieChart, 60 nœuds) + **clôture Epic #344**. **10/10 SP = 100% velocity, 36e sprint consécutif.** **Bilan global Epic #337+#344** : 141 → 17 violations (-88%), 2648 → ~413 nœuds (-84%), **60 → 0 critical (-100%) ✅**, 7 → 2 règles distinctes. Reste 17 serious color-contrast dispersé = dette acceptée non bloquante. Critère Epic #344 « <10 violations » non atteint (17) mais critère critical=0 atteint = objectif principal. Pré-allocation versions 15e application S17→S36 (5 versions séquentielles 0 conflit). **Bonus infra CI (#345)** : cache Playwright `actions/cache@v4` sur `~/.cache/ms-playwright` ajouté à visual.yml + a11y.yml + perf.yml (fix root cause hang `npx playwright install --with-deps chromium` qui prenait 44min en download Chrome for Testing v147 sur runner GitHub — économie 10-15min/run en cache hit) + bump visual timeout 30→45min (120 baselines). Anomalies S36 : (a) **AI-36.1** subagent /dev #345 timeout stream à 117 tu sans RESULT (récidive AI-35.1) — mitigation parent §3d-5 reprise idempotente complète (commit + push + PR + lint diacritics) en ~10 min. Branche correctement nommée `fix/#345-...` cette fois (AI-35.2 ✅ résolu via prompt explicite) ; (b) **AI-36.2** /dev+/review #347 cumulé 172 tu pour scope minimal 3 edits — /review adversarial trop verbeux ; (c) **AI-36.3** workflows CI (visual + a11y + perf) tous victimes hang playwright install — fixé en S36 mais à hisser dans templates claude-config ; (d) #346 38 tu, #347 172 tu, #348 38 tu, #349 32 tu — budgets dispersés. Mike est parti dormir mid-sprint → mode autonome déroulé sans interruption, gestion des 3 incidents CI + 1 timeout subagent sans réveil. Bilan §4 publié : retro `docs/retros/sprint-36.md` (à venir), velocity.json maj S36=100%, ARCHITECTURE.md version courante v2.65.0, milestone GH #36 fermé.
- 2026-05-26 — **Sprint 35 livré (Dette a11y DS — WCAG AA)** : 3 PRs DS séquentielles (arbitrage Mike up-front : zéro risque conflit > parallélisme) — **#341 (#338) v2.64.3** ARIA quick wins (button-name/select-name/aria-required-attr/aria-prohibited-attr, ~210 nœuds, 14 fichiers : 5 HTML pages + components.js + bumps version) ; **#342 (#339) v2.64.4** color-contrast tokens (4 chaînes recalibrées WCAG AA : `--code-comment` tous thèmes/modes ~600 nœuds, `--accent-light` light mode MSYX/Nhood ~900 nœuds, badges `--*-light` `[data-mode=light]` ~250 nœuds, `--text-dim` Nhood light ~30 nœuds, `.bottom-nav-item.active` 120 nœuds, retrait `style=color:var(--accent)` formulaires.html 5 nœuds — total ~1900 nœuds résolus, baselines VR option B différée) ; **#343 (#340) v2.64.5** labels forms + scrollable-region (13 fixes, capit doc `DS-PRINCIPLES.md` §3.1 « Label vs aria-label » 6 cas + 2 anti-patterns, ~300 nœuds résolus). **11/11 SP = 100% velocity, 35e sprint consécutif.** **Bilan a11y axe-core** (rapport `docs/audit-a11y-2026-05-25.md` régénéré) : 141 → 78 violations (-45%), 2648 → 1003 nœuds (-62%), 60 → 12 critical (-80%), 5 règles restantes. Critère Epic « <10 violations » NON atteint → **Epic #344 + milestone Sprint 36** créés pour cleanup résiduel (5 sub-catégories : color-contrast 48 runs/871 nœuds dominant, scrollable-region 12 runs, aria-required-children 6 émergence, label 6 runs/54 nœuds, aria-prohibited-attr 6 runs/60 nœuds). Pré-allocation versions 14e application S17→S35 (v2.64.3/4/5 séquentiels, 0 conflit). 0 hook block-merge friction. Anomalies S35 : (a) **AI-35.1** subagent /dev #340 a violé règle « NE PAS watch CI » + omis ligne RESULT formatée (94 tu, texte libre sortie sur statut CI) — mitigation parent §3d-5 reprise idempotente (état GitHub OK : PR existe, branche `worktree-agent-a3c88118c55ce1f06`, 4/5 verts) → continuation propre ; à hisser claude-config prompt /dev : bannir `gh pr checks` post-push + exiger RESULT TOUJOURS ; (b) **AI-35.2** subagent #340 push sur branche `worktree-agent-*` au lieu de `feat/#340-*` ou `fix/#340-*` — convention naming feature branch perdue, à investiguer dans /dev skill ; (c) **AI-35.3** subagent /dev #339 a livré option B (baselines différées) au lieu de option A (régénérées dans la PR) malgré recommandation parent — pas de blocage CI mais drift visuel potentiel non détecté ; (d) **AI-35.4** subagent /dev #339 a consommé 107 tu (juste sous seuil 100+ ANOMALIE), RESULT retourné OK mais signal alerte budget refactor mécanique tokens couleur (Edit replace_all=true aurait économisé). Bilan §4 publié : retro `docs/retros/sprint-35.md` (à venir), velocity.json maj S35=100%, ARCHITECTURE.md version courante v2.64.5, milestone GH #35 fermé.
- 2026-05-11 00:00 — **Sprint 32 livré + déployé (Brand identity + polish theming)** : 3 PRs DS séquentielles — **#270 (#266) v2.54.11** polish .btn-danger/.btn-success/.btn-warning theme-aware Option C hybride (tokens fg dédiés `--btn-on-danger/success/warning` calibrés WCAG AA + shadow alpha modulable `--btn-shadow-alpha` 35%/22% + border subtil) ; **#271 (#265) v2.55.0** mode switch iOS-style (`.mode-switch`, `role="switch"`, animation slide, icônes Lucide sprite `i-sun`/`i-moon`, tactile WCAG 2.5.5 ≥ 44×44, renommage `updateModeButtons` → `updateModeSwitch`, showcase fondation.html) ; **#272 (#268) v2.56.0** brand identity (wordmark `design-system` Space Grotesk 600 18px gradient charte en header à côté du pictogramme MSYX 40×40 maison-mère + mark `DS` 72px Space Grotesk 700 sur login + nouveau module `shared/css/components/brand.css` 27e module + section `pages/fondation.html#brand` + entrées registry `brand-wordmark` & `brand-mark-ds`, ancienne entrée SVG renommée `brand-logo-svg`). **11/11 SP = 100% velocity, 32e sprint consécutif.** Pré-allocation versions 13e application consécutive S17→S32 (3 versions séquentielles v2.54.11/v2.55.0/v2.56.0, 0 conflit subagent). **Arbitrage upfront Mike** 3e application pattern S23 #192 pour #268 (4 décisions visuelles figées en commentaire GitHub avant /dev — 0 re-arbitrage pendant dev). Anomalies S32 : (a) **récidive 3e fois** « subagent oublie git push » sur lot 1 (#266 + #265) — mitigation parent ~5 min/PR push + create PR manuel — pattern S24 #209 + S25 #218 + S32 ; **anomaly warning explicite** injecté dans prompt lot 2 → #268 push-and-return OK (120 tool_uses, RESULT propre) ; (b) **limite rebase chain S31 identifiée** : `--theirs` aveugle sur 7 fichiers a écrasé modifs cross-PR sur `nav.js` (mode-switch perdu), `layout.css` (styles `.mode-switch` perdus), `components-registry.json` (classes mode-switch supprimées + entrées brand-* perdues) → mitigation parent ~15 min merge intelligent fichier par fichier ; capit à hisser claude-config (AI-32.1 distinguer fichiers version pure vs fichiers logique cross-PR). Friction : visual VR a FAIL post-merge (108 baselines mutent comme attendu — wordmark + mark DS + mode-switch impactent toutes les pages), régénération locale via `pnpm install` + `npm run test:visual:update` (30 min × 1 worker) + commit `a4b4475` + push, CI main all-green post-régen. Bilan §4 publié : retro `docs/retros/sprint-32.md` (4 actions S33 : AI-32.1 rebase chain durcissement, AI-32.2 push obligatoire en bloc dédié, AI-32.3 préservation JSON registry, AI-32.4 workflow update-baselines.yml dispatchable), velocity.json maj S32=100%, milestone GH #33 fermé. **Déployé 00:00** : v2.56.0 prod sur design-system.msyx.fr (deploy_tag `deploy-20260511-000013`, previous `deploy-20260510-204339`), gh release v2.56.0 publiée (https://github.com/msyx-dev/design-system-project/releases/tag/v2.56.0), registry.json _global maj (commit `dd86dfe`). Audit sécu pre-deploy : VERDICT OK (969 lignes ajoutées, 0 pattern dangereux détecté, diff CSS additif + JS template literals + nouveau module brand.css). Smoke test prod OK (302 auth gate sur /). Cumulé 6 bumps depuis prod v2.54.10 (v2.54.11 → v2.55.0 → v2.56.0). DS = static file_server, déploiement = git pull (déjà sync local = serveur).

